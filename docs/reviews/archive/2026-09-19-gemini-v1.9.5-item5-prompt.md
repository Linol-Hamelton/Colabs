# Gemini Item 5 Dispatch Prompt - A5 Documentation Closure and Track B Registry

**Date**: 2026-09-19  
**Implementer**: Gemini  
**Auditor**: DeepSeek - audits Item 5 before its commit  
**Plan of record**: `docs/reviews/2026-09-19-deepseek-flash-consolidated-v1.9.5-plan-r2.md`, sections 4.A5 and 5 (B1-B4)  
**Preconditions**: Item 4 committed as `032efeb` (owner push pending; do not push); `PROTO-DEC-0029`..`0032` live; owner decision D3 in force: single append-only `docs/decisions/REGISTRY.md`, enforcement **WARN-first**, JSON twin only if generated in CI.  
**Decision number for this item**: `PROTO-DEC-0033` (draft in the report; controller transcribes after owner approval).

---

## Part A - A5 documentation closure

1. **Legacy evidence wording** (`.ai/docs/PROTOCOL.md`, the evidence section around lines 160-175): add one sentence where legacy receipts are described: legacy unauthenticated receipts are editable and not tamper-evident; `- entry hash format: 2` is the only authenticated format; upgrade by recording a new entry. Keep the existing fail-closed and `--allow-legacy` text.
2. **Nonce check (verify-and-close)**: grep the canonical docs (`AGENTS.md`, `.ai/docs/*.md`) for `nonce`. Current tree has no hits; if a stale "16-byte" claim appears anywhere, correct it to the actual 32-byte (64 hex) nonce from `protocol-hooks.cjs` (`crypto.randomBytes(32)`), and report both the grep result and any fix.
3. **Clean-tree wording (verify-and-close)**: grep for "clean tree" / "clean working" claims; the working pattern (untracked artifacts are digest-visible) must not be contradicted. Report the grep result.
4. **P5-F2 deviation paragraph** (`.ai/docs/PROTOCOL.md`, archive/evidence section): one short paragraph recording the batch-boundary deviation so it is not rediscovered: when a second batch is archived into an existing `ARCHIVE.md`, the boundary entry's canonicalized body must hash-match its `- entry:` label; the defect was fixed and is pinned by `tests/archive.test.cjs` "P5-F2: second batch boundary in ARCHIVE.md remains valid across archive batches and verify --deep". Reference `docs/reviews/2026-09-18-deepseek-flash-p5-gate-review.md`.

## Part B - Track B registry (B1-B4, WARN-first)

### B1/B3 - Create `docs/decisions/REGISTRY.md`

- Header explains: append-only rows; the current status of an id is its **last** row; existing rows are never edited; status changes are new rows.
- Table columns: `| id | status | reopen-trigger | frozen-at | supersedes | evidence |`.
- Statuses: `accepted`, `frozen`, `reopened`.
- `reopen-trigger` values (taxonomy): `invariant-broken`, `metric-drop`, `new-external-data`, `security-finding`, `owner-directive`, `higher-source-contradiction`, `none`.
- Seed one row per existing decision id: extract every `### (PROTO-)?DEC-\d{4}` id from `.ai/DECISIONS.md` (32 ids including `PROTO-DEC-0032`); legacy ids get `status: accepted`, `reopen-trigger: none`, empty `frozen-at`/`evidence`; copy `supersedes` when the block carries one.
- `frozen-at` carries a commit SHA when an id is frozen; `evidence` carries `path#anchor` or a hash when relevant.
- Do **not** edit `.ai/DECISIONS.md`; the registry is a new file. The controller adds the `PROTO-DEC-0033` row when transcribing that decision.

### B2 - Trigger taxonomy and reopening rule

Document in AGENTS.md section 6 (one short paragraph): reopening an accepted decision requires a recorded trigger row in the registry; no trigger, no reopening; doubt stays a TASK/journal note. The registry is append-only and is covered by the shared-document lock (add `docs/decisions/REGISTRY.md` to the locked-file rule in the same paragraph). Tightened trigger semantics (metric baseline, reproducible external data, dated owner directive, PoC for security, contradicting path for higher-source) are documented in the same paragraph in one sentence each.

### B4 - Validator checks (`validate-protocol.ps1`, `role: source`, WARN-first)

Add a registry block after the decision-block checks:

1. Registry missing -> `[WARN]` (migration safety), not FAIL.
2. Coverage: every id in `.ai/DECISIONS.md` has at least one registry row; missing -> `[WARN]`; registry ids not present in the decision log -> `[WARN]`.
3. Immutability: compare `docs/decisions/REGISTRY.md` with `git show HEAD:docs/decisions/REGISTRY.md`; a removed or modified existing row -> `[WARN]`.
4. New decision blocks: an id present in the working copy but not in `git show HEAD:.ai/DECISIONS.md` must carry `Reopen-trigger:` with a taxonomy value; missing or unknown -> `[WARN]`.
5. No FAILs in this item; a later decision can upgrade enforcement. Keep the checks fast, side-effect free, ASCII-only; never write to DECISIONS.md or the registry.

### Tests

New `tests/registry.test.cjs` (register it in `protocol-manifest.json`): registry absent -> WARN; missing id -> WARN; extra id -> WARN; edited row vs HEAD -> WARN; new block without `Reopen-trigger:` -> WARN; unknown trigger -> WARN; complete valid registry -> PASS. Use the existing fixture helpers; keep runtime short.

### Docs

`.ai/docs/PROTOCOL.md`: a short "Decision registry" subsection (format, append-only rule, the four WARN checks, the trigger taxonomy reference).

## Optional micro-fix R-1 (from the A3 re-audit)

In `protocol-handoff.cjs` `runCheck`, set `PROTOCOL_SKIP_GATE` only for the validator check (`check.quick === true`), not for the regression suite or custom host checks. Include only if the diff stays trivial; note it in the report either way.

## Decision draft

`PROTO-DEC-0033`: the registry format, append-only rule, trigger taxonomy, WARN-first validation, and the lock extension. Present the text in the report; do not insert it (the controller transcribes after owner approval and adds its registry row in the same locked write).

## Acceptance and evidence

- `node --test tests/registry.test.cjs`, plus gate/handoff/session/validator/archive suites; full `test-protocol.ps1` green; `validate-protocol.ps1` exit 0 with 0 warnings; registry covers all ids.
- Report includes the A5 greps (nonce, clean-tree, legacy wording) and the P5-F2 paragraph diff.
- Journal entry (five labels), `record --owner gemini-434bcd8012e0f38c`, `verify --deep` exit 0; stop for DeepSeek's audit; no commit, no push.

## Rules

- Do not touch decision blocks, other sessions' journals, or items already committed beyond the optional R-1 micro-fix.
- UTF-8 without BOM and LF; `.ps1` ASCII-only.
- Stop and ask on any contradiction with the repository or any red check.
