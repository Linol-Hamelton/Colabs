# Gemini Implementation Prompt - v1.9.5 (Owner Dispatch)

**Date**: 2026-09-19  
**Implementer**: Gemini (owner directive: `gemini` = implementer)  
**Plan of record**: `docs/reviews/2026-09-19-deepseek-flash-consolidated-v1.9.5-plan-r2.md` (Revision 2, section numbers below refer to it)  
**Auditor**: DeepSeek (`deepseek` = controller, reviewer, auditor) - audits every item before the next one starts  
**Baseline**: HEAD `a6dbf8c`; tag `v1.9.4` -> `c71bdcf`; validator exits 0 with **0 warnings** (28 journals; Track 0.1 executed by the controller on 2026-09-19); the v1.9.4 certification package was frozen, re-recorded, committed and pushed under D5/D8  
**Conflict**: Gemini did not author the plan and may implement it; Gemini may not certify its own work, mark task completion, or edit shared documents (`TASK.md`, `PLAN.md`, `DECISIONS.md`, `ARCHIVE.md`).

---

## Owner confirmation (confirmed 2026-09-19, RuslanFomenko)

- **D1 supervisor policy**: (a) any live PID>4 - CONFIRMED.
- **D2 RECENT_WINDOW**: 15 minutes, empty journals and snapshots - CONFIRMED.
- **D3-D10**: all section-9 recommendations accepted; no exceptions. D5, D8, D9, D10 remain owner-executed or owner-authorized actions; the Gemini-Flash and Qoder artifacts are downgraded to advisory.
- **Track 0 authorization**: granted; Item 0 executed by the controller (see the work order). Gemini verifies the state before Item 1.
- **Commit policy**: no commits during an item; when DeepSeek's audit passes and the owner approves the item, the implementer commits the approved item atomically (owner instruction, this dispatch).
- **Track C**: deferred; do not start it.

The confirmations above close the decisions the plan needs; no further owner questions are required to start Item 1.

---

## 0. Your role and the rules of engagement

You are the implementer. You work one item at a time, produce code plus tests plus evidence, then stop for the audit. DeepSeek audits adversarially; the owner approves the gate; only then does the next item start. You do not:

- mark any item or the task complete;
- fabricate `Approved by:` text or edit `.ai/DECISIONS.md` (draft decision text for the owner to approve);
- edit `.ai/TASK.md`, `.ai/PLAN.md`, `.ai/ARCHIVE.md` or another session's journal without the shared-doc lock and explicit owner text;
- commit or push unless the owner's dispatch says so;
- run `record` for any owner other than your own;
- do cross-item refactors, renames or formatting of files you are not changing.

Stop and ask when a decision is missing, the validator is red, a change conflicts with `AGENTS.md` or `DECISIONS.md`, or a plan defect is found. Never silently deviate from the plan; record the issue and ask.

## 1. Read first (in order)

1. `AGENTS.md` (sections 2, 4, 5, 6, 7, 8, 11, 12).
2. `.ai/TASK.md` and `.ai/DECISIONS.md` (`PROTO-DEC-0025`, `PROTO-DEC-0028`).
3. The plan of record, especially sections 0-1 (state and facts), 3 (Track 0), 4 (A1-A5), 7 (sequencing), 8 (acceptance), 9 (decisions), 10 (gate protocol).
4. `docs/reviews/2026-09-19-deepseek-flash-final-plan-probe-review.md` (reproductions and line references).
5. `docs/reviews/2026-09-19-gemini-opus-final-plan-adversarial-review.md` (the deltas already folded into the plan).
6. `.ai/docs/PROTOCOL.md` around lines 60-75 and 190-200; `templates/reviews/REVIEW.md`; `tests/session.test.cjs`, `tests/handoff.test.cjs`, `tests/helpers.cjs`.

## 2. Start-of-session procedure

1. `git status --short --branch`; confirm HEAD `a6dbf8c` and note unexpected changes (another session may be active).
2. `powershell -ExecutionPolicy Bypass -File .\validate-protocol.ps1`; expected `Protocol OK. 1 warning(s).` (32 journals). Any FAIL is a stop condition.
3. Start your protocol session: `node .ai/bin/protocol-session.cjs start --agent gemini --root D:\Colabs`. Keep the printed owner id and token; they are yours for the lock and evidence.
4. Report the baseline to the owner (one short message) and proceed to Item 0.

## 3. Work order (one gate per item)

| Item | Scope | Gate |
|---|---|---|
| 0 | Track 0 housekeeping (plan section 3, 0.1-0.3) - **executed by DeepSeek 2026-09-19**: journals 28/30, validator 0 warnings; v1.9.4 package frozen, re-recorded, committed and pushed | verify the state, then proceed to Item 1 |
| 1 | A1 C0 liveness fix + tests + PROTOCOL.md + PROTO-DEC-0029 draft | 11-branch matrix green; manual repro fixed; DeepSeek audit; owner approval |
| 2 | A2 journal-cap automation + the `record` ordering fix + CI escalation | regression test for the record-order defect; 0 warnings; audit |
| 3 | A4 capability/evidence discipline + `REVIEW.md` template fields | tests per plan A4; audit |
| 4 | A3 `gate-check` subcommand + validator integration + `tests/gate.test.cjs` | plan A3 test list green; audit |
| 5 | A5 documentation closure | diff review; audit |
| 6 | B1-B4 registry, then C1/C2 only when the owner dispatches them | per-item audit |

Do not start an item before the previous gate closes. The final freeze, ordered record pass, commit, tag and push are owner actions; you do not perform them unless explicitly instructed.

## 4. Item 0 - Track 0 housekeeping (EXECUTED 2026-09-19 by DeepSeek)

Status: executed under owner authorization; Gemini verifies, does not repeat. Verify: the index-rule journal count is <= 30; `validate-protocol.ps1` exits 0 with 0 warnings; `git log -1 --oneline` shows the certification commit. Then proceed to Item 1. The procedure is retained for future rounds:

1. To remove a journal: `node .ai/bin/protocol-archive.cjs worklog .ai/worklog/<journal>.md --keep 0 --root .` then `node .ai/bin/protocol-session.cjs prune --root .`, then `git add -A -- .ai/worklog/<journal>.md` (the validator counts index entries).
2. Never delete text: it moves to `.ai/ARCHIVE.md`; the emptied file is quarantined to `.ai/runtime/pruned/`.

## 5. Item 1 - A1 C0 liveness fix (detail)

**Defect**: `protocol-session.cjs:28-33` (`isProcessAlive`) reads only `state.pid`; `prune` (`:182`) and `cleanup-runtime` (`:247`, `:261`, `:272`) never consult `supervisorPid`, so a session with a live supervisor loses its empty journal and snapshot.

**5.1 `isSessionAlive(state)` contract** (replace `isProcessAlive` at the four call sites; keep one implementation, reuse from `protocol-lock.cjs` per plan A1.5):

| State | Result |
|---|---|
| hostname mismatch (foreign host) | `null` |
| `supervisorPid` integer > 0 and alive | `true` |
| `supervisorPid` integer > 0 and dead | fall through to `pid` |
| `pid` integer > 0 and alive | `true` |
| `pid` integer > 0 and dead | `false` |
| no usable pid fields (legacy null state) | `null` |

PID liveness: `process.kill(pid, 0)`; `EPERM` counts as alive; other errors dead.

**5.2 Call-site polarity** (per site, not a rename):

| Call site | `true` | `false` | `null` |
|---|---|---|---|
| `prune` empty-journal check (`:182`) | skip (even with `--force`) | quarantine | preserve; `--force` may quarantine with an audit message |
| `cleanup-runtime` live check (`:247`) | skip | continue to TTL/force rules | preserve (even with `--force`) |
| `cleanup-runtime` dead-session check (`:261`) | skip | existing `--force`/24h rule | preserve |
| `cleanup-runtime` stale check (`:272`) | skip | existing 7d rule | preserve |

**5.3 Recency fallback**: when liveness is `false` or `null` and the artifact is an empty journal (prune) or a snapshot (cleanup), treat it as active while `mtime >= now - RECENT_WINDOW` (D2 confirmed: 15 minutes, both artifact classes, documented as a heuristic). Journals with entries stay protected by `holdsContent`. `--force` overrides recency but never a live session.

**5.4 Supervisor registration**: accept any live integer PID > 4 for `--supervisor-pid` (D1 confirmed: option a). Rationale recorded in the plan (registration is not an authenticated boundary; own/ppid-only blocks shell-spawned orchestrators). Document the anti-accident model.

**5.5 Mandatory tests** (`tests/session.test.cjs` unless a dedicated cleanup test file exists). Age artifacts with `fs.utimesSync` beyond the window unless stated:

1. supervisor alive, transient dead, aged -> journal + snapshot preserved (a recency-only implementation must fail here)
2. supervisor dead, transient dead, aged -> journal quarantined, snapshot removable
3. supervisor dead, transient alive -> preserved
4. no supervisor, dead, aged -> prunable/removable
5. no supervisor, dead, fresh -> preserved by recency
6. foreign host, aged -> prune preserves without `--force`; cleanup preserves even with `--force`
7. journal with content, everything dead -> never quarantined
8. `--force` + live supervisor -> preserved (both commands)
9. `--force` + foreign host -> prune may quarantine (audited), cleanup preserves
10. missing state file, empty journal fresh / aged -> preserved / quarantined
11. certify two entries, archive the older (`--keep 1`), `verify --deep` -> exit 0

**5.6 Docs and decision**: update `.ai/docs/PROTOCOL.md:68-73` and `:194-196` in the same change (the current text promises "an active live session ... is never pruned", which the defect breaks). Draft `PROTO-DEC-0029` text in your implementation report; do not insert it into `DECISIONS.md` without the owner's `Approved by:` line.

**5.7 Acceptance commands** (exact):
`node --test tests/session.test.cjs`, `powershell -ExecutionPolicy Bypass -File .\test-protocol.ps1`, `powershell -ExecutionPolicy Bypass -File .\validate-protocol.ps1` (exit 0, 0 warnings), plus a manual clone reproduction of the probe scenario showing the journal and snapshot survive with a live supervisor. Write the journal entry, `record`, `verify --deep`, and hand the item to DeepSeek.

## 6. Evidence and reporting format

For every item, in this order: (1) journal entry with the five labels; (2) `node .ai/bin/protocol-handoff.cjs record --owner <your owner id>`; (3) `verify --owner <your owner id> --deep` (must exit 0); (4) a short report to the owner: item, files changed, commands run with real exit codes, evidence path, blockers, next step. If a check fails, the Evidence records the failure; fix the tree and re-record rather than hand-waving.

## 7. Stop conditions

- D1/D2 (or any decision the item needs) not confirmed.
- Validator red, journal over 150 lines, or count over 30 after your change.
- A change would touch a shared document without the lock and owner text.
- A conflict with `AGENTS.md` or a decision block; a plan defect; or an ambiguity that changes behavior.
- A test cannot be written without weakening another acceptance branch.
