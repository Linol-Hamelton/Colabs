# Gemini Item 3 Dispatch Prompt - A4 Capability and Evidence Discipline

**Date**: 2026-09-19  
**Implementer**: Gemini  
**Auditor**: DeepSeek - audits Item 3 before its commit  
**Plan of record**: `docs/reviews/2026-09-19-deepseek-flash-consolidated-v1.9.5-plan-r2.md`, section 4.A4  
**Preconditions**: Item 2 and B5 committed per `docs/reviews/2026-09-19-deepseek-flash-a2-audit.md` section 7; `PROTO-DEC-0030` (approval provenance) and `PROTO-DEC-0029` are live; the plan numbers for this item's decision are now `PROTO-DEC-0031`.

---

## Scope (policy and template only; `gate-check` code is Item 4)

A4 makes certification capability explicit and keeps advisory output from ever carrying gate weight. Implement:

1. **Review template** (`templates/reviews/REVIEW.md`): add these header fields, documented in the template body:
   - `**Mode**: CERTIFYING | ADVISORY`
   - `**Receipt-Owner**: <owner id>` (a `Session:` field is accepted as a legacy fallback)
   - `**Receipt**: <path or digest>` (optional)
   A CERTIFYING mode requires all four capabilities: FS_WRITE, SHELL_EXEC, EVIDENCE_SIGN, REPO_READ. Capability is set by the orchestrator profile, never self-declared.

2. **Mandate text** (AGENTS.md section 2, "Mandatory Adversarial Peer Review Prompt"): state that (a) a certifying verdict requires the four capabilities and a review header carrying `Mode: CERTIFYING` with a `Receipt-Owner`; (b) advisory outputs carry `[MODE: READ-ONLY ADVISORY]` and are persisted only through the section 5.5 transcription fallback, always marked non-certifying; (c) a `FAIL` or `BLOCKED` verdict requires at least one reproduction per claim, otherwise it is advisory and cannot reopen a decision. Keep the section's existing meaning; add, do not rewrite.

3. **PROTOCOL.md**: one short subsection describing the mode/capability model and how a reviewer receipt binds a certifying review to its owner (the mechanical check is Item 4's `gate-check`; do not implement it here).

4. **Test**: add one focused test (for example in `tests/validator.test.cjs` or `tests/review-findings.test.cjs`) asserting the template contains the `Mode`, `Receipt-Owner` and `Receipt` fields and that AGENTS.md carries the four capability names. This pins the policy text against silent removal.

5. **Decision draft**: `PROTO-DEC-0031` (Status: Accepted template, approved-by left for the owner), covering the capability model, the mode field, the FAIL-needs-reproduction rule, and the advisory transcription route. Do not insert it into `.ai/DECISIONS.md`; present the text in your report for the owner's approval, and the controller will transcribe it under the B5 rule if the owner confirms in chat.

## Acceptance and evidence

- `validate-protocol.ps1` exit 0 with 0 warnings; `test-protocol.ps1` green; the new test passes.
- Journal entry with the five labels, `record --owner gemini-434bcd8012e0f38c`, `verify --deep` exit 0.
- Stop for DeepSeek's audit; commit only after the audit passes and the owner approves.

## Rules

- Do not touch `.ai/DECISIONS.md` (append-only, controller-owned for now), other sessions' journals, or Item 1/2 code.
- No push; UTF-8 without BOM and LF; `.ps1` ASCII-only.
- Keep the change minimal; no unrelated edits.
