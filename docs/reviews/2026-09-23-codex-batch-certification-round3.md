# Codex - ROUND-3 independent certification, slot 2

Mode: CERTIFYING
Receipt-Owner: codex-115d0cc78bdb41eb
Reviewed commit: 4ded1bee1c2acf2392fdeededf50935f59138302
Worktree: D:/Colabs-cert/codex3
Reviewer: Codex, GPT-6
Date: 2026-09-23 UTC
Scope: adversarial negative tests and internal consistency of the second remediation
scope-check: PASS
Verdict: FAIL
HEAD at start: 4ded1bee1c2acf2392fdeededf50935f59138302
HEAD at end: 4ded1bee1c2acf2392fdeededf50935f59138302
Working tree at start: clean; git status --porcelain empty before session start
Working tree at end: dirty, review artifacts and TASK review-state note only; exact status below
Receipt: .ai/worklog/codex-115d0cc78bdb41eb.md, generated by protocol-handoff record

| id | root-cause | requirement | paths | reproduction | exit | severity | disposition | attempt |
|---|---|---|---|---|---|---|---|---|
| R3-C01 | RC-role-prose-substring | Spec section 6 requires negation for the particular role token | .ai/bin/protocol-scope.cjs | node docs/research/2026-09-23-codex-round3/reproduce.cjs negation | 0 | HIGH | confirmed | 1 |
| R3-C02 | RC-role-section-parse | Spec section 6 restricts inputs to actual structured Roles entries | .ai/bin/protocol-scope.cjs | node docs/research/2026-09-23-codex-round3/reproduce.cjs roles | 0 | HIGH | confirmed | 1 |
| R3-C03 | RC-owner-source-guessing | Spec section 6 requires the candidate journal actual producer identity; unknown exits 2 | .ai/bin/protocol-scope.cjs | node docs/research/2026-09-23-codex-round3/reproduce.cjs journal | 0 | HIGH | confirmed | 1 |
| R3-C04 | RC-cross-file-scope | Spec section 4 limits discovery to docs/reviews findings files plus the target | .ai/bin/protocol-verdict.cjs | node docs/research/2026-09-23-codex-round3/reproduce.cjs cross | 2 | MEDIUM | confirmed | 1 |
| R3-C05 | RC-immutability-boundary | AGENTS section 6 forbids editing a written decision block | validate-protocol.ps1 | node docs/research/2026-09-23-codex-round3/reproduce.cjs immutability | 0 | HIGH | confirmed | 2 |

## Determination

Five reproduced protected-path defects prevent certification under PROTO-DEC-0041 item 4. No implementation changes were made. RC-immutability-boundary still fails on its recorded attempt 2 of 2; return that exhausted root cause to the owner rather than opening another remediation attempt.

The prescribed single-line malformed ledger directions no longer silently drop a protected finding. All 60 malformed-shape CLI executions returned 2 (30 shapes, each with and without --stop-rule). This does not certify the candidate: the independence checks still produce false PASS, and existing decision text can change undetected.

## R3-C01 - clause-wide negation suppresses an affirmative excluded role

Location: .ai/bin/protocol-scope.cjs:423 and :430. The first negation anywhere before a matched word suppresses that role. Only the first occurrence of each role pattern is inspected.
Fixture TASK has the structured entry `- reviewer: not reviewer, controller`; review declares reviewer-123 and producer builder-456. Controller is affirmative and must exit 1.

Command: `node docs/research/2026-09-23-codex-round3/reproduce.cjs negation`
Exact output, exit 0:
```text
Independence check PASS: Reviewer 'reviewer-123' is independent of producer 'builder-456'.
```
Additional executed cases: `not author and is the controller`, `not author of the old candidate, but author of this candidate`, and `not only author but also certifier` also exit 0. Conversely, `not author but not controller` incorrectly exits 1. Full inputs and outputs are in roles-results.json.

## R3-C02 - example or similarly named section shadows the actual Roles section

Location: .ai/bin/protocol-scope.cjs:330. The unanchored split and startsWith('Roles') select the first textual match, including fenced examples and `## Roles archive`.
The minimal fixture has a fenced `## Roles` example assigning certifier, followed by the actual `## Roles` assigning controller. The latter is ignored.

Command: `node docs/research/2026-09-23-codex-round3/reproduce.cjs roles`
Exact output, exit 0:
```text
Independence check PASS: Reviewer 'reviewer-123' is independent of producer 'builder-456'.
```
Expected exit 1. The same false PASS occurs with `## Roles archive` as the decoy. Reversing the roles produces a false rejection, and a fenced entry inside the true Roles section is also treated as an assignment.

## R3-C03 - mismatched fence marker exposes an example identity

Location: .ai/bin/protocol-scope.cjs:278. A boolean toggles on either marker without remembering the opening marker or its length.
The entire journal example is enclosed by four tildes; two triple-backtick lines are ordinary content within that fence. The first backtick line incorrectly ends exclusion, exposing `- recorded: 2026-09-23 by builder-456`. No actual producer identity exists outside the example.

Command: `node docs/research/2026-09-23-codex-round3/reproduce.cjs journal`
Exact output, exit 0:
```text
Independence check PASS: Reviewer 'reviewer-123' is independent of producer 'builder-456'.
```
Expected exit 2. Four-backtick outer fences with shorter triple-backtick content also bypass the guard. Ordinary triple-backtick and tilde examples correctly exit 2; a real same-owner receipt after an example correctly exits 1.

## R3-C04 - cross-file discovery exceeds the owner's specified corpus

Location: .ai/bin/protocol-verdict.cjs:729. Besides repository docs/reviews, discovery scans the target's own directory and its nested docs/reviews directory.
Fixture: a valid `scratch/target.md` ledger, plus unrelated `scratch/unrelated-findings.md` prose; no findings files in repository docs/reviews. Only the target is in scope, so check 2 must exit 0.

Command: `node docs/research/2026-09-23-codex-round3/reproduce.cjs cross`
Exact stderr, exit 2:
```text
BLOCKED: Malformed findings ledger: no table header row found
```
The same rejection occurs for `scratch/docs/reviews/unrelated-findings.md`. The spec clause at docs/specs/2026-09-23-executable-rulebook-spec.md:134 matches the owner's corpus boundary; the implementation widens it. This is separate from union/contiguity arithmetic, which passed its probes.

## R3-C05 - non-last decision blank changes remain invisible

Location: validate-protocol.ps1:1014 and :1029. Normalization strips trailing separators/newlines from every block, but the added raw comparison covers only the last committed block when the block count is unchanged.
Fixture: commit two accepted decisions, then add one blank line after the first body's final line. No decision, separator or heading is appended. The validator accepts the mutation. Deleting a blank line from the same boundary also exits 0.

Command: `node docs/research/2026-09-23-codex-round3/reproduce.cjs immutability`
Exact wrapper output, exit 0:
```text
[PASS] 2 committed decision blocks are unchanged
Protocol OK. 1 warning(s).
```
The wrapper prints these two lines from the real validator and propagates its status; complete validator stdout/stderr and command are retained in repro-immutability-result.json. Expected exit 1. The incidental fixture warning is unrelated to immutability.

## Executed coverage and limits

- Durable scripts and raw results: docs/research/2026-09-23-codex-round3/. Run `node docs/research/2026-09-23-codex-round3/probes.cjs all` to regenerate; results preserve each child command, expected/actual exit, stdout and stderr. Fixtures are confined to this worktree's .ai/runtime/codex-round3/.
- Initial matrix: 201 checks, 16 exploratory expectation mismatches. Expanded roles run: 32 checks; immutability run: 13 checks. Mismatches were adjudicated, not automatically promoted to defects. Five minimal reproductions above independently reconfirmed the findings.
- Ledger shapes: framed row above header; unframed/list/numbered/inline-code/backtick-fenced/tilde-fenced/quoted/heading/comment rows above and below; blank/prose splits; second tables; duplicate headers; short/long cells; invalid exit/disposition; nonnumeric/fractional/zero attempt all exit 2 in both modes. Protected probes use neutral `observed mismatch`, not invariant/contract wording.
- Disposition case variants are normalized: `CONFIRMED`, `Confirmed`, `cOnFiRmEd` exit 1 in verdict mode and 0 in stop-only mode, just like lowercase. The dispatch asked for exit 2; the repository spec enumerates dispositions without prescribing case sensitivity. This difference is recorded, not claimed as a dropped finding: verdict mode still blocks the confirmed protected finding. Case strictness needs an explicit contract if intended.
- Exploratory fully fragmented row with all pipes removed from its body was ignored as post-table prose (exit 0). This is not a demonstrated table-row bypass: all nine cells became separate pipe-free lines, indistinguishable from permitted trailing prose. No RC-ledger-parse failure is asserted from that artificial form.
- Both committed union ledgers exit 0 in verdict mode and in --stop-rule mode; the latter reports 13 groups. Cross-file {1,2} and duplicate identical dispositions pass; {2}, {1,3}, conflicting dispositions and a malformed selected ledger exit 2; {1,2,3} triggers stop with exit 1.
- All six excluded roles exit 1 individually. Standing-certifier wording, ordinary negations, and prose outside Roles pass. Unknown flags, flag-like typos and stray positionals exit 2 on both tools.
- Every managed/source manifest entry and .ai/.claude/.codex custom paths yields FAIL/1 with a neutral requirement. Off-protected tests/docs/lookalikes yield RECOMMENDATION/0. Absolute, drive-relative, UNC and parent-segment forms exit 2. Manifest absent/empty/wrong-type keys or invalid entries exit 2; root missing, ancestor borrowing and nested shadowing were tested.
- Touched-set probes include staged rename, tracked edit, untracked Unicode/plus paths, scope/forbidden input modifications, exclusions and prefix collisions. Out-of-scope inputs and forbidden untracked files are reported. Missing-scope diagnostics are repo-relative and byte-identical across distinct checkout paths. Index boundPaths returns Cyrillic and plus tokens.
- Immutability positive/negative execution: legitimate append with and without separator succeeds; actual body edit, deletion, internal separator addition/removal, heading edit, trailing space and blank addition to the last block fail. The non-last-block regression above remains open.
- Scope: `git diff --name-status 89ce192bf6923d00b0328378be8c4b73fd47234b HEAD` contains only protocol-scope.cjs, protocol-verdict.cjs, validate-protocol.ps1, their two test files, the spec and round3 ledger. No unapproved implementation surface was found in that remediation delta.

## Validation, provenance and closing state

Independently ran `powershell -NoProfile -ExecutionPolicy Bypass -File ./validate-protocol.ps1`: exit 0, 0 warnings. Initial `test-protocol.ps1`: exit 1, 375/376, 379358.4067 ms. The one failure was the fixture assertion `a directory named .git does not prove a valid working tree`: with TEMP/TMP inside this worktree, Git instead discovered the enclosing repository, so validation rejected it with `protocol root is inside another repository` rather than the expected message. This is a fixture-environment mismatch, not a false-green candidate defect.

Set GIT_CEILING_DIRECTORIES to the same local TEMP directory and reran `node --test --test-name-pattern='a directory named .git does not prove a valid working tree' tests/validator.test.cjs`: exit 0, 1/1. The full final-tree suite is rerun by mandatory protocol-handoff record with this boundary; its actual exit codes are in this report's own generated journal receipt. No suite result substitutes for the five reproductions.

Session started explicitly. Owner-authorized invocation used `-s danger-full-access` because workspace-write prevents Node spawning git. TEMP/TMP stayed inside the worktree. No files in D:/Colabs were opened; no other ROUND-3/shadow report was read. The named Claude ROUND-2 report and second remediation session journal are absent from the checked-out inventory; tested their claims from the union ledger/source. The available Gemini journal bc8f87d66fd614fa is first-remediation history. TASK's older status prose is stale against this candidate and owner dispatch.

Corpus measured before writing: 59 files / 592028 bytes, so the free slot was reserved without moving history. No 2026-09-23 batch certification was archived. Line anchors were verified with git grep -n on the reviewed candidate (1-based). No decision/registry/history edits, commit, push, or implementation repair.

End `git rev-parse HEAD`: 4ded1bee1c2acf2392fdeededf50935f59138302. End `git status --porcelain`:
```text
 M .ai/TASK.md
?? .ai/worklog/codex-115d0cc78bdb41eb.md
?? docs/research/2026-09-23-codex-round3/
?? docs/reviews/2026-09-23-codex-batch-certification-round3.md
```

Verdict arithmetic check: `node .ai/bin/protocol-verdict.cjs docs/reviews/2026-09-23-codex-batch-certification-round3.md` returns `Verdict: FAIL`, exit 1, driven by R3-C01..R3-C05; agrees with this certification. Attempt values preserve the existing root-cause history; no third remediation is authorized by this report. No Completed transition is made.
