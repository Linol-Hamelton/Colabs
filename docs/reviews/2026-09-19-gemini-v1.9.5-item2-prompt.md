# Gemini Item 2 Dispatch Prompt - Finish Item 1, then A2 (Owner-Approved Gate)

**Date**: 2026-09-19  
**Implementer**: Gemini (`gemini-434bcd8012e0f38c`)  
**Auditor**: DeepSeek (`deepseek-flash-8a681a17d5224abf`) - audits Item 2 before its commit  
**Plan of record**: `docs/reviews/2026-09-19-deepseek-flash-consolidated-v1.9.5-plan-r2.md` (revision 2), section 4.A2  
**Item 1 audit**: `docs/reviews/2026-09-19-deepseek-flash-a1-audit.md` - verdict PASS, follow-ups AUD-1 (required) and AUD-4 (with Item 2)  
**Owner gate decision (2026-09-19, direct confirmation)**: Item 1 gate approved; apply AUD-1 before committing Item 1; AUD-4 may be done with Item 2; PROTO-DEC-0029 approved and already recorded in `.ai/DECISIONS.md` by the controller.  
**Conflict**: Gemini implements; Gemini does not certify its own work.

---

## Step 0 - Finish Item 1 (before any Item 2 code)

### 0.1 Apply AUD-1 (documentation wording, one paragraph)

In `.ai/docs/PROTOCOL.md`, the `prune` paragraph currently reads: "If liveness cannot be verified (foreign host or unknown PID state), the journal is preserved during standard runs and quarantined only under `--force` with an explicit audit warning." That contradicts the observed and specified behavior for a **missing or unreadable** state file (it falls to the recency window, then is quarantined without an audit warning; the plan's A1.6 branch 10 requires this).

Reword to state both cases precisely, for example:

> If the session state is present but liveness cannot be verified (for example a foreign host), the journal is preserved during standard runs and quarantined only under `--force` with an explicit audit warning. If the state file is missing or unreadable, the empty journal is treated as unknown liveness and falls back to the recency window; once it is older than `RECENT_WINDOW` it is quarantined.

Keep the rest of the paragraph (live session, lock holder, `holdsContent`) unchanged.

### 0.2 Verify and commit Item 1 atomically

1. Run: `node --test tests/session.test.cjs` (31/31), `powershell -ExecutionPolicy Bypass -File .\test-protocol.ps1` (212/212), `powershell -ExecutionPolicy Bypass -File .\validate-protocol.ps1` (exit 0, 0 warnings).
2. Stage exactly the Item 1 record (do not include unrelated files):
   `git add -A -- .ai/bin/protocol-session.cjs .ai/bin/protocol-lock.cjs .ai/docs/PROTOCOL.md tests/session.test.cjs tests/review-findings.test.cjs .ai/DECISIONS.md .ai/TASK.md .ai/worklog/gemini-434bcd8012e0f38c.md .ai/worklog/deepseek-flash-8a681a17d5224abf.md docs/reviews/2026-09-19-deepseek-flash-a1-audit.md`
3. Commit once, no amend, no history rewrite:
   `git commit -m "fix(protocol): C0 liveness - supervisor-first three-way model (PROTO-DEC-0029)"`
4. Do **not** commit `docs/reviews/2026-09-19-deepseek-flash-mcp-selection-analysis.md` or this prompt (the controller handles those separately). Do **not** push; the owner pushes.
5. Report the commit hash and the check outputs, then proceed to Item 2.

---

## Step 1 - Item 2 (A2): record ordering fix, cap invariant, CI escalation

### 1.1 `record` ordering defect (plan fact 11) - the primary code change

Observed defect: `protocol-handoff.cjs` `record` calls `autoArchiveWorklog` **before** attaching its own Evidence block (about 14 lines). A journal at ~145 lines therefore ends at 159 lines and the validator fails (`[FAIL] ... exceeds limit of 150`), leaving the tree red until someone archives manually.

Required behavior:

1. Compute the final size before writing: journal content with the newest entry's Evidence block replaced/attached. If the result would exceed 150 lines, archive older entries first (existing `autoArchiveWorklog` with keep-newest), then re-check.
2. If the newest entry plus preamble plus its fresh Evidence still exceeds 150 lines, fail with an actionable message (for example: "journal entry is too long; split the entry before recording") and leave the journal exactly as it was - no partial write, no lost Evidence.
3. A normal record must never leave a journal over 150 lines.

Tests (add to `tests/handoff.test.cjs`, using existing fixtures):

- Journal at limit-minus-10 lines: after `record`, length <= 150 and `verify --owner --deep` exit 0.
- Simulated oversized single entry: `record` exits non-zero with the actionable message and the journal is unchanged.
- Auto-archive still keeps the newest entry with its fresh Evidence and moves older entries to `.ai/ARCHIVE.md`.

### 1.2 Cap invariant and CI escalation (owner decision D6)

- The journal count is evaluated over the index: `git ls-files --cached --others --exclude-standard` filtered to `.ai/worklog/[^/]+\.md` minus `README.md`. Removing a journal is a three-step procedure: `protocol-archive.cjs worklog <path> --keep 0` -> `prune` -> `git add -A -- <removed path>` (quarantine alone leaves the path in the index and the warning persists).
- **No CI workflow exists today** (`.github/workflows/` is absent). Create a minimal `.github/workflows/protocol.yml`:
  - triggers: `push` to `main` and `pull_request`;
  - runner `windows-latest`, `actions/checkout@v4`, `actions/setup-node@v4` with Node 22;
  - step: run `powershell -ExecutionPolicy Bypass -File .\validate-protocol.ps1`, capture output, exit non-zero if it contains `[WARN]` or `[FAIL]`;
  - optional second step: run `test-protocol.ps1` (recommended; if it makes the workflow too slow for the owner, leave it commented out with a note).
  - No other actions, no secrets, no network calls beyond the actions themselves.
- Add a regression test or a scripted check for the three-step removal procedure only if it fits the existing test fixtures; otherwise document it in PROTOCOL.md (1.3).

### 1.3 Documentation

In `.ai/docs/PROTOCOL.md`, document: (a) the record ordering guarantee from 1.1; (b) the cap maintenance procedure from 1.2 (keep-0 + prune + stage; index-based count). Keep it short.

### 1.4 AUD-4 regression tests (from the Item 1 audit)

- Corrupt/unreadable state JSON with an aged empty journal -> `prune` quarantines it (unknown liveness + expired recency).
- Active lock holder's empty journal -> `prune --force` preserves it.

### 1.5 Acceptance and evidence for Item 2

- `node --test tests/session.test.cjs`, `node --test tests/handoff.test.cjs`, full `test-protocol.ps1` green; `validate-protocol.ps1` exit 0 with 0 warnings; journal count <= 30.
- End-to-end proof for 1.1 in a throwaway clone or fixture (near-cap journal), included in the report.
- Journal entry with the five labels, `record --owner gemini-434bcd8012e0f38c`, `verify --deep` exit 0, then **stop** for DeepSeek's adversarial audit. Commit Item 2 only after the audit passes and the owner approves.

## Hard rules

- Do not modify `PROTO-DEC-0029` or any other decision block; `.ai/DECISIONS.md` is append-only.
- Do not edit another session's journal; do not rewrite history; do not push.
- UTF-8 without BOM and LF in text files; `.ps1` files ASCII-only.
- Stop and ask on any blocker, red check, or ambiguity; do not exceed the Item 2 scope.
