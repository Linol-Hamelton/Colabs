# Closing step d: the final plan (PROTO-DEC-0053 step d)

- Frame `task:vmc-final`, parent-scope `program:validator-migration-council`. Your one role: final synthesiser (owner §27). Read `prompts/COMMON.md`
  first; it binds you.
- The owner names one of the three round-3 synthesisers (README model table); the owner's text names the Anthropic
  flagship (owner §27; PROTO-DEC-0053 step d).

## Inputs

All of round 1, round 2 and round 3, `draft-decision.md`, and both critiques (owner §27).

## Work

1. For every critic finding: ACCEPTED, or REJECTED with evidence and reason, or OWNER DECISION
   REQUIRED (owner §27). A critic can be rejected; a majority can be wrong; reproduction and
   evidence outrank vote count.
   Mark every accepted point with the critic it came from (S-003 step 7). A critic whose point
   shaped the plan is thereby not independent of it when the implementation is certified.
2. Write `final-plan.md` with exactly the sections A to AC of §28, in that order.
3. Section AA holds only the questions that pass the filter of §29; zero is valid.
4. Section S and the task packets follow §30 (every field) and §31 (the anti-big-bang rule).
5. Section AB is a proposed decision block in the form of `.ai/DECISIONS.md`, with
   `Approved by: <owner's words, date; transcribed by ...>` left open. Its `Supersedes:` line
   names exactly what it changes, for example PROTO-DEC-0039 item 3 as to timing, if that is the
   outcome.
6. Check the result against the success criteria of §32 and the final rule of §33. Any outcome §33
   lists is acceptable if the evidence supports it.

## Output

`final-plan.md`. Its length follows §28; the size cap is the owner's to relax (COMMON section 7).
Nothing in it is binding until the owner approves a block (§33).
