# Codex: independent round-3 remediation synthesis

- FACT (session observation): SHA `4ded1bee1c2acf2392fdeededf50935f59138302`; checkout `D:/Colabs`; tree dirty; UTC 2026-09-24.
- FACT (session declaration): model GPT-6; client Codex; owner `codex-2568015b58c2f596`; scope Z1-Z4/D/S/C/K; Mode: ADVISORY; Verdict: RECOMMENDATION.
- FACT (session declaration): I did not open any other round-3 synthesis before writing this report.
- FACT (command record): ran `git rev-parse --show-toplevel`, `git rev-parse HEAD`, `git status --short --branch`, `git log --oneline -10`, `git ls-files`, `Get-Content -Encoding UTF8`, `Get-ChildItem`, `rg -n`; PowerShell here-strings piped to `node` for the read-only reproductions in S-codex-10. Final check commands/results belong to my journal.
- FACT (input record): read BRIEF, ROUND2, PROCEDURE-MAP, all four named round-1 reports and all four named round-2 reports; checked cited source slices directly. Current mapping inputs exist but are untracked in the inventory. No implementation or certification is claimed.
- CLAIM (notation): FACT identifies source-backed observations; CLAIM identifies recommendations/inferences; HYPOTHESIS identifies unmeasured forecasts. Tables inherit their introductory label. Line ranges below are edit locations in this baseline, not promises of future line numbers. A new file has no existing line range.

## S-codex-01 - Recommendation and authorization boundary

FACT: The authorized round includes fixed grammar, bounded collection, producer records, reproductions, dispatch, signals, watchdog and the research/kernel scenario (`.ai/DECISIONS.md:2012`, `2021`, `2022`, `2099`, `2153`, `2218`). The owner, not a synthesis, adopts the final plan (`.ai/DECISIONS.md:2216`).
CLAIM: Deliver these as one integrated remediation candidate, with separate internal blocks and one final parallel certification. Make the interfaces explicit before splitting work. Do not count a proposed grammar as a closed finding or a successful unit test as candidate certification.
CLAIM: Prefer small additions to existing scripts plus one dispatcher, client registry and signals writer. An alternative universal workflow engine would multiply contracts before resolving the measured failures; defer it to redesign.
FACT: High-risk certification excludes candidate authors/controllers and requires two parallel independent reviewers; availability and shadow status do not remove that filter (`.ai/DECISIONS.md:1792`, `1793`, `1975`, `1978`).
CLAIM: Research participation alone does not establish eligibility for subsequent implementation certification. Record who actually specifies, controls and executes the chosen candidate; a fresh session does not erase candidate control. This report occupies no certifying slot.

## S-codex-02 - Z1: roles, agreement and matching

FACT: Current roles parsing chooses a heading by prefix and stores exclusions globally by participant (`.ai/bin/protocol-scope.cjs:330`, `340`, `363`). Both ordering variants of coordinator+certifier retain exclusion in my in-memory reproduction; the certifier-only control passes (`.ai/bin/protocol-scope.cjs:338`, `385`).
CLAIM: Agreement with Mistral on item Z1-04: agree with reservations. Preserve every governance condition, but put explanatory prose in TASK Constraints and universal invariants in AGENTS; do not introduce a second prose parser disguised as a constraint grammar.
CLAIM: Agreement with Gemini on item Z1-03: partially agree. Closed role tokens and fence-aware parsing are appropriate; globally scoped identities and exclusions for entire tables/code blocks can conceal the very dependency or copied analysis being checked.
CLAIM: Proposed Roles grammar: `- <participant> @ <scope-id>: <role>[, <role>...]`. Participant and scope IDs use `[a-z][a-z0-9._-]*`; roles are exactly author, executor, controller, coordinator, implementer, pair-member, certifier, reviewer, auditor. Map pair-member explicitly to the existing executing-pair exclusion. No optional unscoped role, free qualifier, negation or trailing commentary.
CLAIM: The certification package names its scope ID and exact candidate SHA; its fixed participant table maps session owners to participant IDs. Evaluate the set union of all role rows for that scope. An exclusion wins over an asserted certifier role, independent of order. Missing scope/owner mapping, malformed row or multiple active Roles sections exits 2. A known excluded participant exits 1. No prefix-derived identity fallback.
CLAIM: Scope IDs describe the work lineage: changing SHA for a fix does not shed its authors/controllers. Disjoint product work gets a different scope. Renaming a scope requires a recorded mapping preserving prior involvement. Candidate SHA and scope ID remain distinct to avoid embedding a commit's own hash inside itself.
CLAIM: During migration preserve no-unilateral-Completed and candidate independence in AGENTS; retain pilot resumption conditions and slot-selection policy in TASK Constraints with decision references. Replace only current role assignments under the shared lock; keep historical task evidence in history. Do not mechanically translate the stale Codex governance line into a current coordinator assignment.
CLAIM: Agreement grammar: `Agreement with <participant> on item <ID>: <degree>.` followed by `Justification: <nonempty text>` for every degree except fully agree. Degrees are exactly `categorically disagree, with reasons`, `disagree`, `partially agree`, `agree with reservations`, `fully agree`, `absolutely agree, with additional confirmation found`. The last requires an additional source/reproduction reference; software checks its presence, reviewers check its substance. Validate referenced item IDs and participant mappings. Unknown input exits 2.
CLAIM: Match normalized seven-word prose windows as a proposed, shadow-tested rule, not a proven detector. Exempt fixed headers/agreement syntax, path/command tokens and exact source-backed quotations; compare justification text and free-text table cells. Parse fence marker/length without treating every fenced paragraph as trusted code. Report both locations and the matched window. Unclassifiable exemptions exit 2; confirmed prohibited overlap exits 1. A match detects repetition, not causation or intellectual dependence.
CLAIM: Edit map: spec `docs/specs/2026-09-23-executable-rulebook-spec.md:168-186` first; `.ai/bin/protocol-scope.cjs` checkIndependence `211-402` and extractExcludedRole `405-452`; `.ai/TASK.md:44-51`, AGENTS Roles guidance, `templates/ai/TASK.md`, and hook role injection must consume the same syntax. Extend `tests/rulebook.test.cjs` and `tests/hooks.test.cjs`; cover R3-C01/C02/C03/F-R3-02, duplicate/order reversal, cross-scope contamination, same participant/new session, missing mappings, malformed fences, and copied prose hidden in tables or justifications.
HYPOTHESIS (risk): trigger—scope relabeling, migration loss or common technical phrases; likelihood medium; impact—false independence or false rejection; prevention—lineage binding, migration checklist and positive/negative matching corpus; compensation—exit 2 and human source inspection; cost—parser migration plus shadow review; residual—undeclared human control and paraphrase remain undetectable.
HYPOTHESIS (net gain): positive if measured false matches stay manageable; deterministic role handling should remove repeated prose-interpretation failures, while matching keeps a narrower, explicit evidentiary limit.

## S-codex-03 - Z2: collection and exhaustion-to-audit

FACT: Spec collection is the immediate `docs/reviews/*findings*.md` set plus the target; code adds two other directories (`docs/specs/2026-09-23-executable-rulebook-spec.md:134-139`; `.ai/bin/protocol-verdict.cjs:724-745`).
CLAIM: Keep the explicit target, one repository-root reviews directory, sorted deduplication and existing framing/conflict checks. No recursive archive/research discovery. Add tests where an undeclared sibling supplies attempt 1, malformed unrelated files are ignored, and a valid target outside reviews remains included. Print collected paths so the source set is inspectable.
FACT: checkStopRule compares dispositions only to detect conflicting duplicates and stops at maxAttempt >=3; direct calls with contiguous attempts 1/2 return exit 0 for all four tested dispositions (`.ai/bin/protocol-verdict.cjs:635-651`, `675-692`).
CLAIM: Agreement with Copilot on item Z2-4: partially agree. The missing attempt-2 transition is real. However, `deferred-by-owner` alone describes an owner disposition, not general budget exhaustion; it cannot close all unsuccessful second attempts or establish that an audit was handed over.
CLAIM: Agreement with Gemini on item Z2-02: fully agree.
FACT: R3-C05 is an accepted exception with no third attempt; the exhaustion procedure must first be proposed in PLAN and certified (`.ai/DECISIONS.md:2021`).
CLAIM: Add `docs/reviews/<batch>-attempt-results.jsonl`, a fixed append-only attempt-result record keyed by scope/root-cause/attempt/candidate: `running|failed|verified|unknown`, evidence locator and source entry hash. The coordinator records transitions under the batch's single-writer procedure. This distinguishes a still-running second attempt from a failed completed one without guessing from elapsed time. Existing ledgers stay findings records; the result record resolves the event boundary.
CLAIM: After failed attempt 2, emit `FAILED_AUDIT_REQUIRED` (exit 1), block repair dispatch and watchdog resume for that root cause, and persist an audit handoff containing attempts, patches, candidate, failed reproductions, unresolved findings and owner decision references. Unknown/inconsistent results exit 2; verified attempt 2 permits continuation. Any attempt 3 remains a violation unless an explicit new owner premise authorizes it. An accepted exception produces `OWNER_EXCEPTION`, forbids automatic retry and carries its approval reference; it does not masquerade as a fresh failure or a PASS of the defect.
CLAIM: checkStopRule should remain a read-only evaluator. It can emit a terminal result; it cannot itself appoint an auditor, write TASK under lock or prove delivery. The coordinator records the cycle as failed and assigns an independent diagnostic auditor, outside both edit streams; any claimed mandatory defect still needs reproduction. Only the owner accepts a residual or a new premise. Unaffected work continues within the same two-stream cap.
CLAIM: Edit map: `.ai/bin/protocol-verdict.cjs` checkStopRule `628-693`, main `723-778`; spec sections 2/4 `60-89`, `128-145`; `.ai/docs/PAIRED-CYCLE.md:155-166` exhaustion/handoff steps; PLAN compact proposal before implementation; dispatcher preflight consumes the result. Extend `tests/rulebook.test.cjs` with running/failed/verified attempt 2, conflicting results, exception authority and forbidden attempt 3. Dispatcher tests prove no resumed repair after closure. Leave `validate-protocol.ps1` immutability code untouched.
CLAIM: Make PLAN room by moving completed historical wave text intact to archive under the coordinator's lock, retaining references and frozen product metrics. Keep the active exhaustion proposal in PLAN; a larger cap or a different home needs the owner only if preservation cannot fit.
HYPOTHESIS (risk): trigger—ambiguous attempt state, omitted source or forged exception; likelihood medium; impact—premature closure or another forbidden attempt; prevention—explicit result grammar, provenance and corpus tests; compensation—BLOCKED plus audit handoff; cost—one event contract and dispatch preflight; residual—auditor availability and owner response time.
HYPOTHESIS (net gain): positive; termination becomes actionable before attempt 3, without spending another immutability repair attempt or stopping unrelated tasks.

## S-codex-04 - Z3: candidate package, producer Evidence and reproductions

CLAIM: Agreement with Copilot on item Z3-02: agree with reservations. Use a computed diff package, but remove the clean-label prerequisite and avoid two independently implemented receipt verifiers.
FACT: anchor reads snapshot's dirty flag; snapshot excludes journals from its digest but computes dirty from all status entries (`.ai/bin/protocol-handoff.cjs:69-85`; `.ai/bin/protocol-hooks.cjs:117-125`, `156`). record requires an existing dated journal before attaching Evidence (`.ai/bin/protocol-handoff.cjs:711`, `739-741`, `779-783`).
FACT: My journal-only mocked-Git call of the actual snapshot function returns `{files:[],dirty:true}`. Thus an isolated checkout with a newly created session journal is also dirty; isolation alone does not guarantee a clean producer label (`.ai/bin/protocol-hooks.cjs:102-156`).
CLAIM: Clean Evidence is possible in a deliberately prepared quiescent checkout whose journal entry is already committed before recording; it is not a reliable requirement of an active shared checkout, nor the required policy. Do not commit ceremonial journal-only checkpoints to obtain the label. Use frozen candidate content plus authenticated entry/digest/check exits; report dirty accurately.
CLAIM: Package fields: full Baseline SHA (`4ded1be` expanded), Candidate SHA, Scope-ID, computed changed paths and diff hash, ordered closure IDs with ledger paths, producer owner/journal/entry hash, candidate content digest, and whole-tree check receipts. Resolve SHAs as commits, verify ancestry, include added/deleted/renamed files and probe hashes. Produce identical package bytes for both independent certifiers; reviewers record dependency widening reasons.
CLAIM: Capture candidate content digest in a checkout at Candidate and require producer Evidence to match it. Journals remain independently hash-checked; unrelated substantive dirt cannot be waived merely by listing it. Either pause substantive writes through recording/verification or use an isolated candidate checkout. Final closure artifacts receive the producer's final-task receipt; later unrelated changes do not retroactively invalidate it.
FACT: The freshness decision distinguishes certification-time and final-task receipts (`.ai/DECISIONS.md:1822-1825`). Node completion checks already authenticate a review's named owner/entry (`.ai/bin/protocol-handoff.cjs:1296-1377`); PowerShell invokes gate-check (`validate-protocol.ps1:876`).
CLAIM: Extend that binding for producer/package validation in Node; let PowerShell call the same code and test parity, rather than build another Evidence parser. First-round review remains whole-candidate; this remediation compares against `4ded1be`. No certification package may silently rely on untracked probes absent from Candidate.
CLAIM: Reproduction locator is structured data, not a shell command template: repository-relative script, interpreter enum, JSON argv array, expected exit and script hash. Resolve real paths inside the candidate, reject parent/absolute/drive/UNC escapes and symlink escapes, require a regular file; no automatic execution while parsing. Missing/invalid locator exits 2. Preserve `none` for advisory observations, never for binding FAIL/BLOCKED proof.
CLAIM: Sanctioned external borrowing adds location, SHA-256 and approved decision/item naming that practice. Validate the sanction in repository state; make a hash-identical in-repository copy the default execution input. If retained externally, verify bytes/access in an explicit reproduction step and persist the result. No network fetch in a gate, and a URI string alone is not a verified reproduction.
FACT: Deterministic rulebook checks require repository-only inputs and no network (`docs/specs/2026-09-23-executable-rulebook-spec.md:30-39`). The external borrowing exception requires recorded sanction and location (`.ai/DECISIONS.md:2022`).
CLAIM: Edit map: template `templates/reviews/REVIEW.md:3-12`; spec `60-89`, `168-186`; handoff anchor/record `62-134`, `704-791` and gateCheck `934-1412`; scope producer resolution `254-320`; verdict parseFindingsLedger `271-490` gains locator validation. Tests: `tests/handoff.test.cjs`, `tests/gate.test.cjs`, `tests/validator.test.cjs`, `tests/rulebook.test.cjs`; dirty-journal-only positive, substantive dirty negative, stale/missing producer, altered entry, absent closure ID, malicious argv/path, borrowing hash/sanction mismatch. PAIRED-CYCLE artifact order and PROTOCOL handoff guidance follow.
HYPOTHESIS (risk): trigger—wrong baseline, moving tree, unsafe borrowed script or hidden unchanged dependency; likelihood medium; impact—false certification or unrepeatable proof; prevention—candidate/digest binding, safe locator and dependency widening; compensation—repackage/reproduce and reject inconsistent Evidence; cost—one package build plus already-required full checks; residual—semantic defects outside tests, local identity trust and external artifact availability.
HYPOTHESIS (net gain): positive but unmeasured; record package-build time, reviewer read time, widened files and escaped defects for the first run. Do not promise numerical savings or remove independent full-tree checks.

## S-codex-05 - Z4: wake-then-fail, bound to one session

FACT: 0051 defines five minutes without artifact size change, up to three same-session resumes, then FALLEN; unsupported resume skips wakes (`.ai/DECISIONS.md:2146-2152`). Stop telemetry writes on Stop, not each tool/read/reasoning event (`.ai/bin/protocol-hooks.cjs:603-615`, `621-636`).
CLAIM: Agreement with Gemini on item Z4-14: agree with reservations. Multiple real signals are needed, but CPU use alone cannot override the approved artifact rule: a busy loop consumes CPU without progress. Do not emit synthetic keepalives for a live PID or permission prompt.
CLAIM: Watch one dispatch UUID, exact CLI session ID, process identity/start time, checkout root, report path and journal owner. Track each artifact separately; other sessions' writes must not reset the timer. Truncation/rotation counts only when an adapter establishes the same session's new artifact. Use monotonic elapsed time; retain one hard deadline across resumes.
CLAIM: Proposed per-client adapter inputs and acceptance measurements (none claimed live-verified here):

| Client | Activity source to establish in preflight | Same-session resume contract |
|---|---|---|
| Claude | Its streamed output and exact transcript; report/journal secondary | Versioned help-derived resume argv; prove checkpoint continuity |
| Codex | Its streamed events and exact session transcript; report/journal secondary | Capture actual session ID; test resume without creating a new session |
| agy | stream-json plus payload transcriptPath; exact gemini journal | Verify its help-derived resume invocation and returned identity |
| Copilot | Current dispatch stream/log files, excluding other sessions | Measure log/session ID association, then resume smoke test |
| vibe | UTF-8 streaming output and measured session artifact | Discover/verify resume by ID; keep UTF-8 environment on resume |
| Kilo | Registered IDE/CLI bridge transcript or tool events, if available | Do not invent a standalone command; unsupported adapter records the gap |

CLAIM: At 300 seconds without qualified size change, persist WAKE-1/2/3 and request continuation from the last journal checkpoint. Before each resume, check Z2 terminal state and ensure the prior invocation cannot remain a second writer. Give each wake a fresh five-minute progress window within the original hard deadline. Reset idle on progress, not the total wake budget. No session ID means no safe resume: FALLEN plus procedure-gap.
CLAIM: On third unsuccessful wake, terminate the owned process tree and append one durable FALLEN event with session ID, checkout, timestamps, last checkpoint, artifact sizes, wake/kill outcomes and safe log paths/hashes. Type fall goes to S; artifact excerpts must exclude credentials. A terminal budget stop suppresses wake, and a wake does not increment the remediation-attempt counter.
CLAIM: Long tests should expose actual test milestones/output through the watched stream. Missing output is an observability gap, not proof of zero reasoning; use the mandated wakes and preserve checkpoints. A future CPU-based exemption would require an explicit policy refinement. Test Windows tree termination and PID-reuse guards with owned fixtures; do not claim no-orphan guarantees without measurement.
CLAIM: Edit map: new `.ai/bin/protocol-dispatch.cjs` activity/supervise/resume/terminate functions; use `docs/research/2026-09-24-remediation-mapping/prompts/launch-round2.cjs:54-116` (activity, kill, run) only as starting material; new `tests/dispatch.test.cjs` fake clock/client fixtures plus controlled Windows child-tree test. CLI-AGENTS sections 3/6/7, PAIRED-CYCLE `155-166`, registry and manifest follow. Avoid adding Stop calls as per-turn heartbeats.
HYPOTHESIS (risk): trigger—silent long operation, missing ID, unrelated writes or duplicate resume; likelihood medium; impact—lost work or concurrent writers; prevention—per-session vectors, qualified output, persisted wake budget and single-writer resume; compensation—checkpoint recovery and investigated FALLEN; cost—adapter probes plus lifecycle tests; residual—opaque remote reasoning and process-tree escape remain possible.
HYPOTHESIS (net gain): positive; stalled runs become bounded, recoverable and diagnosable without treating process existence as productive work.

## S-codex-06 - D: dispatcher and execution profiles

FACT: The temporary launcher builds shell command strings, groups journals by participant prefix and stops on first stall (`docs/research/2026-09-24-remediation-mapping/prompts/launch-round2.cjs:28-40`, `54-69`, `86`, `109`). The approved procedure requires prompt files, verified flags, a hard bound and a client registry (`.ai/DECISIONS.md:2085-2099`).
CLAIM: New `.ai/client-registry.json` holds schema version, client/provider/version/date, executable or shim type, verified model/effort mappings, actual-setting observation method, argv templates, required environment, permission profile, hard cap, session-ID parser, artifact adapter, resume template and known failure/countermeasure records. Unknown capability/version exits 2; explicit unsupported resume follows Z4 instead of guessed flags.
CLAIM: Dispatch reads a checked repository prompt path and sends only the fixed ASCII file-reference line. Spawn executable+argv without a shell where possible; an unavoidable Windows shim needs a separately tested adapter. Verify cwd before launch and in the prompt. Validate paths against traversal/shell injection and refuse unknown options. Log requested versus observed model/effort separately; unavailable observation is unknown, never the requested value relabeled as actual.
CLAIM: Failed process restart and idle wake are different counters. Keep three wakes inside a session; a failed run may restart once per 0050. A second failure goes to the owner; the fixed-dispatcher-defect exception carries its concrete fix/evidence reference. A resume never extends the hard deadline or bypasses budget closure. Persist transitions before launching, so supervisor restart cannot duplicate work.
CLAIM: Edit map: new protocol-dispatch functions loadProfile/buildInvocation/preflight/supervise; new client-registry; `.ai/docs/CLI-AGENTS.md:14-48`, `69-91`, `123-155`; manifest managed/test entries. Tests `tests/dispatch.test.cjs`, `tests/manifest.test.cjs`, `tests/installer.test.cjs`: UTF-8, spaces/metacharacters, wrong repo, stale profile, unknown flag, restart limits, partial failure, missing prompt/report and report line caps. Installed profiles must require local executable resolution, not ship workstation-specific paths as defaults.
HYPOTHESIS (risk): trigger—version drift, shell quoting or excess grant; likelihood high without preflight; impact—lost attempts or out-of-scope actions; prevention—versioned argv fixtures and measured permission profile; compensation—exit 2 plus procedure-gap; cost—one smoke test per supported adapter/version; residual—client-internal enforcement remains a separate trust boundary.
HYPOTHESIS (net gain): positive; one verified invocation contract replaces repeated hand-built commands. Retain runtime metrics/logs as operational observations, never certification authority.

## S-codex-07 - S: append-only signals and batch processing

FACT: Signals require type/date/participant/evidence/cost/disposition, permit any participant to append and escalate groups left open for two batches (`.ai/DECISIONS.md:2129-2145`).
CLAIM: Use `.ai/SIGNALS.jsonl` with strict schema v1 and `protocol-signals.cjs append|validate|plan`. Required event fields: id, UTC date, participant, type (`procedure-gap|script-candidate|fall`), repository evidence path, cost `{attempts,minutes,ownerInterventions}` using numbers or explicit unknown, disposition (`open|scheduled|closed|owner-required`), rootCause, batchId and optional supersedesEvent. Reject unknown fields/invalid references with exit 2; corrections append a linked event, never rewrite one.
CLAIM: Serialize appends through a short dedicated writer transaction, preserving complete UTF-8/LF records; do not assume concurrent append calls are atomic. A malformed tail blocks processing and preserves its bytes for an explicit repair event/procedure. Deduplicate retries by event ID without suppressing separate falls. Store durable evidence for FALLEN outside disposable runtime before closing its signal.
CLAIM: At each batch plan the coordinator groups open signals by root cause, records coverage/cost and a proposed procedure/profile patch. One procedure-gap suffices to propose action; only the owner adopts it. For script candidates record pass/fail of all four spec conditions and the unmet condition if retained as model work. Append each batch consideration so two consecutive open batches are computable. Do not silently auto-adopt the proposal.
CLAIM: Edit map: new `.ai/bin/protocol-signals.cjs`, `.ai/SIGNALS.jsonl`, `.ai/docs/SIGNALS.md`; CLI-AGENTS cross-reference and spec section 1 `28-58` linkage; manifest and state initialization via installer templates. New `tests/signals.test.cjs` covers concurrent writers, retry IDs, malformed tails, append-only corrections, durable evidence and two-batch escalation. Do not propagate the source repository's signal history into new hosts.
HYPOTHESIS (risk): trigger—interleaved append, invalid evidence or permanent open queue; likelihood medium; impact—lost signals or bureaucratic accumulation; prevention—serialized schema validation and per-batch dispositions; compensation—preserved tail, repair record and owner escalation; cost—small writer plus batch review; residual—root-cause grouping and adoption remain human judgments.
HYPOTHESIS (net gain): positive if the batch review replaces recurring incident rediscovery; measure interventions and repeat failures, not raw signal volume.

## S-codex-08 - C: research and kernel-change scenario

FACT: Research permits up to three rounds with no self-challenge, independent third-round syntheses, then the 0053 draft/two critiques/final-plan closing (`.ai/DECISIONS.md:2178-2185`, `2211-2218`). PAIRED-CYCLE's one-synthesis wording is still unqualified (`.ai/docs/PAIRED-CYCLE.md:160`).
CLAIM: Add a scenario in PAIRED-CYCLE: owner frame and zone owners; round-1 reports; independent round-2 cross-challenges and one synthesis; owner-named three syntheses without mutual reads; owner-named drafter; two mutually independent critiques; owner-named fixer answering every critique; explicit owner adoption before implementation. Missing report stays pending, never silently counts as agreement. Design critique and implementation certification are separate gates.
CLAIM: Record the map's layer scheme as a navigation aid, not an adopted kernel architecture. Enforce report headers, stable point IDs and 250-line caps at dispatch handoff. Keep the independence statement in each journal; scripts can verify its presence, not prove absence of reading. A prompt/report cap failure requests a new compliant artifact without rewriting historical reports.
CLAIM: Edit map: `.ai/docs/PAIRED-CYCLE.md:155-166` plus new scenario section; `.ai/docs/CLI-AGENTS.md:143-155` dispatch fields; PROTOCOL paired-cycle entry `354` links to it; scenario fixtures in `tests/operator.test.cjs` and dispatch artifact checks. No new all-purpose workflow engine or extra automatic research rounds.
HYPOTHESIS (risk): trigger—draft treated as approval or missing critique ignored; likelihood medium; impact—unauthorized kernel implementation; prevention—explicit advisory statuses and named artifact/owner transitions; compensation—hold dispatch and return unresolved choices to owner; cost—two bounded critiques already mandated; residual—rubber-stamping and undisclosed cross-reading.
HYPOTHESIS (net gain): positive; the chosen plan exposes disagreements before implementation rather than spending certifier rounds on ambiguous intent.

## S-codex-09 - K: correct the conflict diagnoses and choose canonical homes

FACT: K2's historical Proposed block exists, but 0022 already supersedes it and the registry records that transition (`.ai/DECISIONS.md:599-601`, `1025`, `1041`; `docs/decisions/REGISTRY.md:50`). K3 overlooks the clause expressly retaining the optional digest policy (`.ai/DECISIONS.md:1612-1613`; `.ai/docs/PROTOCOL.md:325-338`).
CLAIM: K2 needs no rewrite of an immutable block and no new licensing decision. Have the map/index display effective status plus provenance; retain raw historical status. K3 needs an operator clarification separating the closed experiments from the retained advisory command, not deletion or a reopened experiment. No Repomix run is needed to establish this textual result.
FACT: K4 is a stale operational description relative to 0049/0051 (`.ai/docs/PAIRED-CYCLE.md:155-165`; `.ai/DECISIONS.md:2054`, `2146-2152`). The agy exception is explicitly client-specific (`.ai/DECISIONS.md:2056-2059`).
CLAIM: Align K4 through Z4/D. For K5 record enforcement capability per profile. Narrow cwd, a Git hook or post-run status is not a security boundary against arbitrary shell commit/push. Require a measured deny mechanism or explicitly approved isolation/permission profile; unverified general bypass exits 2. Do not copy agy's grant to vibe/Copilot. No user-level config edits in this research dispatch; later external edits have one designated owner and backups.
CLAIM: Seven canonical operational homes below remain subordinate to DECISIONS; other copies become short links/examples rather than competing normative definitions:

| Rule | Canonical home | Alignment/test surface |
|---|---|---|
| Escalation budget | PAIRED-CYCLE section 5 | PLAN references it; spec section 4 defines machine encoding; attempt-boundary tests |
| Idle/liveness | CLI-AGENTS watchdog procedure | PAIRED-CYCLE links it; registry supplies adapters; fake-clock tests |
| Findings ledger format | Executable-rulebook spec section 2 | PAIRED-CYCLE section 4/review template link schema; parser fixtures; ship schema reference to hosts |
| Verdicts and review modes | AGENTS section 2 | PROTOCOL/template link definitions; vocabulary and capability fixtures |
| Completion gate | AGENTS section 2 | PROTOCOL has invocation instructions; shared Node/PS gate contract tests |
| Paired cycle | PAIRED-CYCLE section 2 | PROTOCOL links scenario; operator fixtures |
| Journal rules | AGENTS section 5 | PROTOCOL/CLAUDE/client guides link it; session/handoff tests |

CLAIM: Edit map: AGENTS sections 2/5, PAIRED-CYCLE `141-166`, PROTOCOL `123-153`, `251-311`, `325-365`, CLI-AGENTS `123-155`, review template and CLAUDE references. New schema material referenced by installed guides must be delivered through manifest; otherwise link a managed schema copy with generated parity, not an absent source-only file. Use existing context-policy/operator/manifest/installer tests for affected references. Preserve historical reports/decisions unchanged; any map correction is a new artifact.
HYPOTHESIS (risk): trigger—deduplication drops a rule, installs a broken link or upgrades a prompt restriction into a security claim; likelihood medium; impact—host drift or false authority; prevention—seven-row migration checklist, installed-fixture checks and measured enforcement flags; compensation—retain old canonical content until parity is verified; cost—bounded documentation pass; residual—future cross-document drift.
HYPOTHESIS (net gain): positive; fewer competing instructions without changing the authority hierarchy or reopening closed decisions.

## S-codex-10 - Disputed FACTs: reproductions and limits

FACT: Stop-rule probe ran the exported function with rows `{root-cause:'RC-probe',_attemptNum:n,attempt:String(n),disposition:d}`, n=1,2. For d=confirmed/unresolved/deferred-by-owner/fixed-and-verified every result was exitCode 0. This supports Copilot's present-code diagnosis; Gemini's proposed attempt-2 behavior is a feasible change, not existing behavior (`.ai/bin/protocol-verdict.cjs:628-692`).
CLAIM: Agreement with Gemini on item Z2-04: agree with reservations. An evaluator can be extended, but closure requires a failed-attempt event and coordinator handoff, not merely an unresolved row. DeepSeek's Z2 section supplies no numbered synthesis item; this reference does not invent one.
FACT: Reproduction of the clean-label disagreement used the actual snapshot function extracted into `vm`, with Git mocked to return only `?? .ai/worklog/probe.md`; output had no digest files and dirty=true. Real `anchor(process.cwd()).dirty` also returned true. These are a unit-level counterexample plus shared-tree observation, not a live isolated-checkout test (`.ai/bin/protocol-hooks.cjs:117-156`; `.ai/bin/protocol-handoff.cjs:77-85`).
CLAIM: Agreement with Mistral on item Z3-02: partially agree. Reject the shared clean-label prerequisite, but neither a universal impossibility claim nor isolated-worktree cleanliness follows automatically. S-codex-04 specifies the attainable content guarantee. Its separate parser criticism also needs correction: the cited range is the findings parser, a reasonable place for the proposed addition.
FACT: Source search and direct reads place parseFindingsLedger at 271 and checkStopRule at 628; the former reads reproduction as data. Copilot's Z3-02 describes an addition at that parser, not an existing locator resolver (`.ai/bin/protocol-verdict.cjs:271`, `343`, `628`; `docs/research/2026-09-24-remediation-mapping/copilot-z3-diff-cert.md:44`).
CLAIM: Agreement with Mistral on item Z3-01: disagree. Searching the challenged report for DeepSeek/shadow/certifier-list text returned no such assertion; its numbered item concerns diff and Evidence (`docs/research/2026-09-24-remediation-mapping/copilot-z3-diff-cert.md:14-34`). Candidate-specific exclusion remains correct policy, but this challenge is misattributed.
FACT: No imported replacement parser was tested: the two role-order probes executed checkIndependence's actual function body in `vm` with virtual TASK/review reads; both returned exit 1, while certifier-only returned 0 (`.ai/bin/protocol-scope.cjs:330-340`, `385-401`).
CLAIM: Round-2 Z4 differences in sandbox access do not prove contradictory client capabilities. No live six-client resume/kill experiment ran here; artifact paths, CPU coverage, restart correctness and cost remain acceptance measurements, not FACTs inferred from telemetry counts. Existing Stop metrics cannot establish a universal continuous heartbeat.

## S-codex-11 - Order, batching, deferred work and owner choices

CLAIM: Sequence: (1) finish 0053 synthesis/draft/critiques/final-plan/adoption; (2) coordinator compacts stale PLAN history and freezes interface specs, candidate scope, exclusions and acceptance fixtures; (3) disjoint implementation blocks; (4) integrated tests and package; (5) one frozen diff and two eligible independent certifiers; (6) owner disposition/closure, with final-task producer Evidence. No freeze commit or push occurs without its existing owner authorization being checked.
CLAIM: Two streams: Gemini owns scope/verdict changes and their rulebook tests (Z1/Z2/Z3 locator). DeepSeek owns dispatcher, registry and signals (D/S/Z4) and coordinates documents. After its parallel block, DeepSeek integrates Z3 package/handoff binding; Gemini then supplies its scope integration. One nominated writer handles spec/TASK/PLAN, manifest and shared test registration; neither stream certifies the candidate it shaped. Prefer contracts agreed first to concurrent edits of the same file.
CLAIM: Dependencies: Z1 scope/identity and Z3 package are specified together; Z2 result contract precedes dispatch retry/resume; S append contract precedes FALLEN integration; D profile identity precedes Z4; C governs approval before code. K reference alignment lands with its owning contract. Fake-client dispatch tests and rulebook parser tests may run independently; full integration/freeze follows both streams.
CLAIM: Acceptance includes source/installed fixtures, all authorized round-3 closures, the retained R3-C05 exception, malformed-input exits, historical failure corpus, shadow/manual comparison, full validator/suite Evidence and protected-path review. The batch is not split into artificial low-risk edits to reduce reviewer count. If the next frozen candidate fails, return reproduced blockers and consumed budgets to the owner before any new automatic round.
CLAIM: Defer to `docs/research/2026-09-23-kernel-architecture/`: universal layer/workflow engine, semantic memory/graph retrieval, validator-language rewrite, generalized receipt redesign, role-rating automation, cost-tool installation and automatic journal-writing service. They are not needed for these bounded contracts. Preserve raw-byte immutability tooling as audit input only; no disguised third R3-C05 repair. Product work remains on hold under 0048.
CLAIM: Owner-only decisions: adopt/amend the final plan; name the draft author and final fixer; appoint two truly independent available certifiers after contribution roles are known; authorize any new external borrowing, client bypass or user-level configuration change; decide exhausted-root-cause/batch continuation or exceptions. If explicit scope/result/lineage contracts here are rejected, choose replacements before implementation, not through parser guesswork.
CLAIM: Grammar field spelling, archive-preserving PLAN compaction, Node delegation from PowerShell, dispatcher location and 0/1/2 exits are proposed implementation choices for the final plan, not separate permission questions. No numerical gain is claimed before measurement; no unfinished work is marked Completed.
