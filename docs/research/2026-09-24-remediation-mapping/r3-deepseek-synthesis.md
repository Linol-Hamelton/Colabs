# Round-3 synthesis - DeepSeek (independent)
- Baseline SHA: `4ded1bee1c2acf2392fdeededf50935f59138302`; tree: dirty (50 entries in `git status --short`: live journals and this round's files). Model: DeepSeek, client: Kilo (IDE session). UTC: 2026-09-24. Owner: `deepseek-face2b2e94e03a81`.
- Commands run: `git rev-parse --show-toplevel`; `git status --short --branch`; `git log --oneline -3`; `node .ai/bin/protocol-session.cjs start --agent deepseek`; the `path:line` reads cited below; per-agent row count of `.ai/runtime/metrics/sessions.jsonl`; read-only listing of `C:\Users\Dmitry\.claude\projects`, `.codex\sessions`, `.gemini\antigravity-cli\brain`, `D:\Colabs-cert\{codex,codex2,codex3,claude2,claude3}\docs\research\2026-09-23-*`; `claude|codex|agy|copilot|vibe --version` and their `--help` filtered for resume/session flags.
- Independence: I did not open `r3-claude-synthesis.md` or `r3-codex-synthesis.md` at any point before this file was written (PROTO-DEC-0052 item 2). One broad grep for the agreement-line pattern returned isolated matching lines from both; I did not use them; recorded in my journal.
- Labels: every repository FACT carries a `path:line` I opened; statements prefixed FACT/CLAIM/HYPOTHESIS are so labelled, and every other design statement in this document is a CLAIM. No code, tests, `.ai/`, other reports, product repositories, lock or commit were touched.

## Z1 - Roles and agreement grammar

**S-deepseek-01. Roles grammar: closed tokens, union merge, scope binding.**
Agreement with gemini on item Z1-06: agree with reservations. Strict token lines are right but incomplete without scope and a merge rule. FACT: `checkIndependence` keeps one `excludedRoles` Map keyed by agent and overwrites per matching line (`protocol-scope.cjs:326,338-341`), and a line matching no excluded token never clears it (`:339`). So in `.ai/TASK.md:46-51`, codex is excluded globally by its coordinator line although `:51` nominates it as certifier, and deepseek resolves to implementer from `:49`. PROTO-DEC-0041 item 1 excludes roles "of that candidate", so last-wins plus global exclusion contradicts it and disables the certifier designations. Grammar (spec section 6): `role_line ::= "- " agent [ " (scope: " scope_id ")" ] ": " role { ", " role }`; roles = author | executor | controller | coordinator | implementer | `member of the executing pair` | certifier | reviewer | auditor; agent = the eight participant names; `scope_id ::= [a-z0-9][a-z0-9-]{2,31}`. `.ai/TASK.md` gains one header line `Task-id: <scope_id>`. Any non-blank line under `## Roles` not matching exits 2; no negation construct exists, so absence of a token is the negation and R3-C01 cannot recur. Merge = ordered union of an agent's lines; unscoped excluded roles always exclude; scoped excluded roles exclude iff the scope id equals the candidate TASK.md's `Task-id`; permitted roles never exclude; missing section/Task-id or unparseable input exits 2 (R3-C02/C03 by line-exact, fence-aware selection replacing `:330` and `:275-285`). Edit map: `protocol-scope.cjs` `checkIndependence` 211-403 (selection 330-331, map 326-345, exclusion 360-394), `extractExcludedRole` 405-452 -> `parseRolesLine` + `evaluateExclusions`; spec 168-186; `tests/rulebook.test.cjs` 804-943. Tests: double-line merge both directions, scope match/mismatch, unknown token, `not`/`without`, `## Roles archive` and 4-tilde decoys (R3-C01/C02/C03/F-R3-02), missing Task-id. Risk: a scoped exclusion leaks via id drift; L med; impact high; prevention - the candidate's TASK.md is the only scope source, ids compared exactly; comp - certifier widening plus the producer binding of S-deepseek-06; cost - fixtures; residual - owner must keep Task-id current. Net gain: the four round-3 independence defects close by construction and the declared certifier slots become usable.

**S-deepseek-02. Where the dropped prose lives; migration; AGENTS.md.**
Agreement with mistral on item Z1-04: partially agree. The dropped text is real, but a second parsed constraint grammar reintroduces a prose parser. The constraints restate binding rules with a canonical home: "no unilateral Completed" = AGENTS.md:77-79 and PAIRED-CYCLE.md:8; candidate-scoped independence = AGENTS.md:89-101 / PROTO-DEC-0041 item 1. Schedule prose ("pilot role resumes", "remains cross-pilot controller") belongs in `## Current state`. AGENTS.md:56-71 must change: it teaches the free-prose form and says at `:69-71` "the role text is free", which PROTO-DEC-0049 item 2 forbids, and its example at `:61-66` breaks the grammar. Replace with the grammar, the `Task-id` line, the scope form, and a note that non-role conditions live outside `## Roles` and are never parsed. Migration of `.ai/TASK.md:46-51` (owner confirms): `gemini: implementer`; `deepseek (scope: tandem-remediation): controller, reviewer`; `codex (scope: tandem-dispatch): coordinator`; `deepseek (scope: vpn-pilot): implementer`; `claude: certifier`; `codex: certifier`. Whether codex's dispatch-preparation role makes it a controller of this candidate is the owner's call (S-deepseek-17); if yes, its scope becomes `tandem-remediation` and slot 2 stays vacant. TASK.md is 74 lines against the 80-line cap (`validate-protocol.ps1:304`), so the coordinator trims stale Open questions in the same pass. Risk: a constraint is lost with no other home; L low after this mapping; impact med; prevention - each is already binding elsewhere and the migration diff is reviewed; comp - git restore; cost low; residual low.

**S-deepseek-03. Agreement lines and the quotation matcher (new check).**
FACT: no agreement checker exists; Gemini's Z1-02 names `checkAgreementLine` at `protocol-scope.cjs:451+`, but 451-576 are return, `parseArgs` and `main`. PROTO-DEC-0048 item 3's hand check applies "until then", so the check is new code: `.ai/bin/protocol-agree.cjs` (managed). Grammar: `Agreement with <participant> on item <item-id>: <degree>`, the six degree strings of PROTO-DEC-0048 item 3, case-normalised, emphasis markers rejected; a non-empty justification after a dot, colon or semicolon separator on the same line is required for every degree except `fully agree`; any other `Agreement with` line exits 2; `--reports` optionally checks referenced item ids. Quotation matcher `--pairs <a> <b> ...`: unit = 8 consecutive normalised word tokens (lowercase, emphasis stripped); exclusions = fenced/inline code, commands, repo paths, `path:line`, decision/finding/root-cause ids, table rows, header lines; exit 1 printing both locations and the window, else 0; intent stays a certifier judgement, refutable in the review; new documents only, historical reports are never re-checked. Tests: one fixture per exclusion class, degree set, missing justification, one quote pair. Risk: false positive on shared phrasing; L med; impact low; prevention - 8-token floor, exclusions, both locations shown; comp - one-line refutation in the review; cost one script; residual med but visible. Net gain: the citation-defect class becomes deterministic for round 2/3 and future research cycles (S-deepseek-12).

## Z2 - R3-C04, budget exhaustion, R3-C05

**S-deepseek-04. Collection scope: one recorded source plus the target.**
Agreement with copilot on item Z2-1 and with gemini on item Z2-01: fully agree. FACT: spec section 4 records `docs/reviews/*findings*.md` plus the target (`docs/specs/2026-09-23-executable-rulebook-spec.md:134-135`); the code builds three directories (`protocol-verdict.cjs:729-733`) while the target is always added (`:726`). Edit: delete lines 731-732; keep `root/docs/reviews` and the target; print the collected corpus. Tests: Codex cross fixture (scratch target + unrelated scratch findings -> exit 0), nested `docs/reviews` ignored, repository ledgers still collected. Risk: a real ledger lived outside the set; L low; impact med; prevention - the spec is the contract, moving one file; comp - output names the corpus; cost none; residual none for compliant trees. Net gain: R3-C04 = F-R3-01 closes; the unrecorded-source class ends.

**S-deepseek-05. `checkStopRule` closes at attempt 2; the procedure hands the case to audit.**
Agreement with copilot on item Z2-4: partially agree. Its `deferred-by-owner` branch is necessary but not sufficient; the general case is a root cause whose highest attempt is 2 with latest disposition `confirmed` or `unresolved`, which today passes silently. FACT: `checkStopRule` reads disposition only for duplicate conflicts (`protocol-verdict.cjs:642-651`) and its only terminal trigger is `maxAttempt >= 3` (`:674-675`, output `:767-775`). Reproduction of the Copilot-Gemini Z2 dispute: the code supports Copilot (re-read 628-778 this session). It can close the cycle's machine state at once; it cannot notify, stop a live executor or file the audit - those stay coordinator steps. Edit: keep `>=3` STOP exit 1; add attempt 2 + {confirmed, unresolved} -> stable first token `EXHAUSTED (exit 1):` naming root cause, attempts, dispositions and the audit handoff; add attempt 2 + `deferred-by-owner` -> `OWNER-EXCEPTION (exit 0):` closed, no further attempt; `fixed-and-verified`/`refuted` at 2 stay PASS. Exit alphabet 0/1/2 unchanged (spec sections 3-4); no code 3. Procedural half (PLAN proposal per PROTO-DEC-0048 item 4, PAIRED-CYCLE section 5 after certification): on EXHAUSTED the coordinator, not the implementer, closes the root cause, stops edits on it, opens `docs/reviews/<date>-<coordinator>-<root-cause>-budget-audit.md` with both attempts' rows, reproductions and diffs, audited by a participant outside the implementing pair; a third attempt needs a new owner decision; R3-C05 stays the accepted exception and byte-comparison/append-only ideas stay audit input, never code. Interface: FALLEN is a fall signal, never an attempt; a resume carries "if your journal records EXHAUSTED for the current root cause, stop and report" (S-deepseek-09). Tests: confirmed/unresolved at 2 -> EXHAUSTED; deferred-by-owner -> OWNER-EXCEPTION; attempt-3 and contiguity unchanged. Risk: `deferred-by-owner` written before the owner decided; L low; impact med; prevention - only an owner decision authorises it, reopening stays gated by a registry trigger row; comp - explicit re-open; cost none; residual - the same human trust boundary as every disposition. Net gain: "close as failed at once" becomes executable at the ledger with no third attempt.

## Z3 - Diff package, producer Evidence, reproductions

**S-deepseek-06. Package, producer Evidence, and the "clean tree" answer.**
Agreement with mistral on item Z3-02: partially agree. The fields are right; the clean-tree requirement is worse than it said. FACT: dirty comes from `git status --porcelain -uall` over the whole tree including `.ai/worklog` (`protocol-hooks.cjs:117-120,156`; rendered `protocol-handoff.cjs:77-79,121`), while only the digest excludes worklog/runtime/ARCHIVE (`protocol-hooks.cjs:125`). So any started session is dirty by its own journal: "clean tree" producer Evidence is unattainable in the shared checkout and effectively everywhere unless the journal is committed before `record`. It is also unnecessary. The binding that works: Candidate commit + entry hash + digest equality; the digest is location-independent because it hashes names and content identities, not paths (`protocol-hooks.cjs:102-158`). Package added to the strict-path header (`templates/reviews/REVIEW.md:3-12`): `Baseline`, `Candidate`, `Diff`, `Closes-findings`, `Producer-journal`, `Producer-Evidence`. Checks in `gateCheck` (`protocol-handoff.cjs:962-1411`), mirrored in `validate-protocol.ps1:584-847`: Candidate 40-hex and equal to worktree HEAD; Baseline its ancestor; the range is the reviewed diff (certifier runs `git diff --no-ext-diff <Baseline>..<Candidate>` locally); every `Closes-findings` id appears in tracked `docs/reviews/**`; Producer-journal resolves and one dated entry names the Candidate; its Evidence entry hash recomputes (existing `checkOwnerReceipt` 574-586, invoked in a new `expectedCommit` mode replacing the current-tree stale check at 628-638); `- anchor:` commit equals Candidate; recorded digest equals the digest computed in the certifier checkout. Any failure exits 2. `record` still attaches the whole-tree validator and suite run. Producer records in an isolated checkout at the frozen Candidate (provisioning: S-deepseek-17). Risk: baseline laundering; L low; impact high; prevention - ancestry, HEAD equality, fixed 40-hex; comp repackage; cost - parser plus fixtures; residual none. Risk: diff-only blindness; L med; impact high; prevention - recorded widening duty plus the attached full run; comp independent reproduction; cost - targeted dependency reading; residual semantic gaps outside tests. Net gain: F-R3-03 closes and both certifiers get one bounded input.

**S-deepseek-07. Reproductions resolve in the repository; sanctioned borrowing is recorded.**
Agreement with copilot on item Z3-04: fully agree, with Mistral's narrowings. Spec section 2 reproduction becomes a locator: `repo:<root-relative path> :: <command using {path}>`; absolute paths, `..`, missing/non-file targets and paths outside the root exit 2; only `{path}` is substituted; `external:<location>; sha256:<64hex>; sanction:<decision-id>` requires all three (readable, hash equal, sanction block names the borrowing) or exit 2, never a silent fallback; `none` stays legal with the RECOMMENDATION ceiling (`spec:117-119`). Rows are historical and never rewritten: FACT the scripts exist only in certifier worktrees (`D:\Colabs-cert\codex3\docs\research\2026-09-23-codex-round3\{probes.cjs,reproduce.cjs}`, `D:\Colabs-cert\claude3\docs\research\2026-09-23-claude-round3\probes.cjs`, with `codex`, `codex2`, `claude2` counterparts) and are absent from `D:\Colabs`; the fix places each at the exact path its row names, in the candidate commit, and re-runs every row's command from the shared checkout into the implementer's journal. Edit: `protocol-verdict.cjs` reproduction cell parsing (~343) + spec section 2. Tests: traversal, missing file, changed hash, missing sanction, `none`. Risk: path drift; L med; prevention - tracked paths and fail-closed resolution; comp fixture rerun; cost - resolver plus fixtures; residual low. Net gain: fourteen unresolvable rows become runnable evidence.

## Z4 - Idle exit and the watchdog

**S-deepseek-08. Per-client activity and resume: measured, not asserted.**
Agreement with qwen on items Z4-02..Z4-07: categorically disagree, with reasons. The uniform-signal claim is refuted by measurement: metrics are per-turn only where the Stop hook fires (`protocol-hooks.cjs:603-614`); `.ai/runtime/metrics/sessions.jsonl` counts claude 104, codex 13, gemini 3, copilot 2, deepseek 1, none for mistral/kilo/vibe (counted this session); journal mtime and session state are session-level (`protocol-session.cjs:6-10`). Measured this session (versions from `--version`, flags from `--help`): claude 2.1.278 - transcript `~/.claude/projects/<cwd-slug>/<session-uuid>.jsonl` (newest 10.5 MB at 2026-09-24T03:24), `-r, --resume`, pre-assignable `--session-id <uuid>`; codex 0.154.0 - `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl`, `codex exec resume <uuid> "<line>"`; agy 1.2.9 - hook payload `transcriptPath` under `~/.gemini/antigravity-cli/brain/<id>/.system_generated/logs/transcript_full.jsonl`; HYPOTHESIS: `agy -p "<line>" --conversation <id>` is the resume combination (verify verbatim from `--help` and a probe at implementation); copilot 1.0.88 - `--log-dir` plus stdout, `-r, --resume`, `--session-id <id>`; vibe 2.25.5 - `--output streaming` stdout, `--resume [SESSION_ID]`, needs `PYTHONUTF8=1` and `PYTHONIOENCODING=utf-8` (`launch-round2.cjs:80-82`); kilo has no CLI (`CLI-AGENTS.md:19-25`), so it is not dispatchable: a stall can only be FALLEN plus a procedure-gap signal. All three transcript roots were listable from this session, so an unsandboxed dispatcher can watch them.

**S-deepseek-09. Watchdog: artifact plus process activity; three wakes; FALLEN.**
Agreement with copilot on item Z4-3 and gemini on item Z4-09: fully agree. Hosts are Windows; no `SIGTERM`/`SIGKILL` code exists in the repository and the launcher kills trees with `taskkill /PID <id> /T /F` (`launch-round2.cjs:71-73`). Stall = no watched-artifact growth for five minutes AND no process-tree CPU advance (two samples of kernel+user time over descendants; a long suite is activity under PROTO-DEC-0049 item 3; Qwen's 270s claim is refuted by the recorded `test-protocol.ps1: exit 0 in 315s` at `.ai/worklog/copilot-2db59a7f7eba7f43.md:43`). On stall: kill the tree, then wake by resuming the session id with the fixed line "Continue from your last journal checkpoint; if your journal records EXHAUSTED for the current root cause, stop and report." up to three times; after the third without progress, FALLEN with logs, session id and wake history, appended as a fall signal; a client without a verified resume gets no wake and is FALLEN on the first stall plus a procedure-gap signal. Hard cap 60 minutes; exit codes 0/1/2; FALLEN is never an attempt (S-deepseek-05). It lives inside the dispatch script (PROTO-DEC-0051 item 5). Tests with a fake client: idle -> kill -> wake -> FALLEN; a long CPU-active child is not a stall; no-resume client. Risk: false stall on a long turn; L med; impact high; prevention - process sampling; comp - wake preserves context and a FALLEN is investigated; cost - polling and two process samples; residual - a silent active client with no artifacts and no CPU is misjudged.

## D, S, C - dispatch, signals, research cycle

**S-deepseek-10. Dispatch script, registry, CLI-AGENTS update (PROTO-DEC-0050).**
New `.ai/bin/protocol-dispatch.cjs` (managed): `run|status|stop|wake`; prompts are repository files and the callee receives one fixed ASCII line; the command is assembled from `.ai/docs/clients.json` (new, source-only data) whose per-client entries carry command template, required env, permission and time flags, model/effort values and tier mapping (PROTO-DEC-0047 item 9), resume and session-id syntax (S-deepseek-08), artefact paths, failure modes with cause and countermeasure, CLI version and verification date (PROTO-DEC-0050 item 3). Fail-closed: each run compares `<cli> --version` with the recorded version and exits 2 on mismatch; prompts outside the repository are refused; idle and cap rules of S-deepseek-09 are enforced; a failed agent restarts once then goes to the owner; start HEAD is recorded and a callee commit is flagged (K5). `.ai/docs/CLI-AGENTS.md` gains the procedure and registry schema; `launch-round2.cjs` remains the recorded temporary workaround. Tests `tests/dispatch.test.cjs` with a fake client. Risk: flag drift; L med; impact med; prevention - version check plus re-verification; comp profile update; cost - one template resolver; residual low. Risk: permission bypass; L low; impact high; prevention - owner-authorised profiles only, CLI-AGENTS section 6 list; comp owner handles violations; cost none beyond the profiles; residual owner-managed. Net gain: one recorded dispatch path replaces hand-quoted commands and their lost attempts.

**S-deepseek-11. Signals ledger (PROTO-DEC-0051).**
New `.ai/SIGNALS.md`, append-only, one line per signal: `Signal: <id> | <type> | <date> | <participant> | <evidence> | <cost> | <disposition>`; id `sig-YYYYMMDD-NNN`; type in {procedure-gap, script-candidate, fall}; evidence repository-relative or `none`; cost `attempts:N|minutes:N|owner:N|unknown`; disposition `open|adopted:<ref>|rejected:<ref>|closed:<ref>`. New `.ai/bin/protocol-signals.cjs` (`add|report|validate`): append validates then writes one line atomically and never deletes; closure is a new line for the same id; a malformed ledger line makes `report` exit 2. No lock for a single-line append (rewriting interleaving is impossible); recorded in the procedure. At batch planning the coordinator runs `report --open`, groups by root cause and proposes the procedure or registry change per group (PROTO-DEC-0050 item 1); a group open at two consecutive plannings escalates to the owner in TASK.md. Journal `Signal:` lines stay valid until the ledger exists. Tests `tests/signals.test.cjs`: grammar negatives, unknown type, id uniqueness, closure by append. Risk: participants stop appending and the ledger stays thin; L med; impact med; prevention - one command, journal lines still allowed, the owner sees open groups at planning; comp - the coordinator re-collects from journals at planning; cost - one script; residual med until adoption. Net gain: recurrence becomes countable in one place instead of scattered journals.

**S-deepseek-12. Research-cycle scenario (PROTO-DEC-0052, 0053).**
New managed document `.ai/docs/RESEARCH-CYCLE.md`: three rounds (zone owners; no self-challenge plus one round-2 synthesis; three independent round-3 syntheses with the no-read journal statement) and the four-step closing (draft by an owner-named synthesiser, independent critiques by the other two in the fixed agreement form, final plan by an owner-named one); advisory until the owner approves. `PAIRED-CYCLE.md:160` is scoped to implementation and certification cycles (PROTO-DEC-0052 item 3) and points to it; PROTOCOL.md points. Register in `protocol-manifest.json` (managed). One reviewer statement suffices (PROTO-DEC-0038 item 2). Risk: the scenario is written but not applied because no dispatcher reads it; L low; impact med; prevention - the round-3 prompt templates and the dispatcher name it; comp - the next research cycle's dispatch cites it; cost - one document; residual low. Net gain: K1's kernel text aligns and this cycle becomes repeatable.

## K - conflicts and canonical homes

**S-deepseek-13.**
K2: FACT `.ai/DECISIONS.md:599-601` (DEC-0014, `Status: Proposed`) violates AGENTS.md section 2 and blocks are immutable; only the owner can approve, withdraw or grandfather it by a new superseding block; only then add a validator warning for an un-superseded `Status: Proposed`. K3: FACT `PROTOCOL.md:325-339` still prescribes the pinned `repomix` digest although PROTO-DEC-0036 (`.ai/DECISIONS.md:1602,1612`) closed Track C permanently; strike the command in 325-339, keep a closure note; the MCP policy 340-346 stands. K4: idle numbers (PROTO-DEC-0047 item 6; 0049 item 3; 0051 item 4; PAIRED-CYCLE.md:155-168): canonical = 0049 item 3 for the threshold and 0051 item 4 for the wake policy; PAIRED-CYCLE section 5 gets one pointer line. K5: the no-commit rule is hard-enforced for agy only (`PROTOCOL.md:71`); in-repo enforcement is the dispatch HEAD check (S-deepseek-10); per-client deny configuration stays owner-managed (PROTO-DEC-0048 item 8). Seven multi-home rules, canonical homes: escalation budget -> PROTO-DEC-0048 item 4 with spec section 4 and `checkStopRule` (PLAN 163-168, PAIRED-CYCLE:159 become pointers); idle/wake -> as K4; findings-ledger format -> spec section 2 for the machine ledger (PAIRED-CYCLE 141-150 renamed as the review-report table and pointing there); verdict vocabulary and review modes -> PROTOCOL.md 251-266 (AGENTS.md section 2 keeps gate consequences; REVIEW.md comments become a pointer); completion gate -> the shared PS/Node contract, prose home PAIRED-CYCLE 304-349, PROTOCOL.md 291-311 reduced to a pointer, AGENTS.md section 2 summary; paired cycle -> PAIRED-CYCLE.md (PROTOCOL.md 358-384 becomes a pointer); journal rules -> AGENTS.md section 5 (PROTOCOL.md 123-151 and CLAUDE.md:10,19-20 become pointers). All are documentation edits: one reviewer statement. Risk: a pointer replaces text that another document still depends on, or two homes keep diverging; L med; impact med; prevention - each edit names the canonical home and keeps the decision blocks untouched (append-only); comp - a later reader finds the pointer and the canonical text together; cost - one documentation pass; residual low. Net gain: seven rules stop being restated in four places, and the kernel text stops teaching rules the decisions already replaced.

## Order, batching, dependencies

**S-deepseek-14.** Interfaces frozen here so the streams never renegotiate while coding: `Task-id`, package field names, signals grammar, stop-rule first tokens, exit alphabet 0/1/2. Batch 1 (parallel): Gemini stream = S-deepseek-01/02/03 (`protocol-scope.cjs`, new `protocol-agree.cjs`, spec section 6, tests); DeepSeek-controller stream = S-deepseek-04/05 (`protocol-verdict.cjs`, spec section 4, tests); the spec is one writer at a time, Gemini first. Batch 2 (parallel): Gemini = S-deepseek-11 then S-deepseek-10/09; DeepSeek = S-deepseek-06/07 (`protocol-handoff.cjs`, `validate-protocol.ps1`, `REVIEW.md`, spec section 2, tests). Batch 3: Gemini = dispatch tests and CLI-AGENTS.md; DeepSeek = S-deepseek-12/13 document pass, TASK/PLAN under the lock (migration, Task-id, PLAN trim with the budget-exhaustion proposal at PROTO-DEC-0048 item 4's named home; trimming stale R1-R8/Proposed sections corrects a stale lower source, AGENTS.md sections 1 and 6; the owner may instead raise the cap). Closure: both streams stop; the coordinator freezes one candidate commit (owner authorises the commit); producer journal + `record` in an isolated checkout; both certifiers receive the identical package and attach their own whole-tree `record`; two independent CERTIFYING reviews outside execution and control (PROTO-DEC-0041 item 2). Codex's limits are recorded exhausted (`.ai/TASK.md:64`), DeepSeek and Gemini control the work, so the second binding slot needs an owner nomination (S-deepseek-17). Note: `docs/reviews/` measured 60 files / 628,588 bytes against the 600 KB reserve; classify-first archiving precedes new review files. Risk: the two streams collide in the spec or the shared documents; L med; impact med; prevention - one writer per file at a time, TASK/PLAN under the lock by the coordinator only, interfaces frozen above; comp - git restore and reapply in order; cost - coordinator attention; residual low. Net gain: one candidate, one package, two parallel streams instead of serial rework.

## Out of scope (deferred to the kernel redesign)

**S-deepseek-15.** The layered packet kernel and conditional loading (`docs/research/2026-09-23-kernel-architecture/BRIEF.md`); metrics/calibration and CodeBurn (PROTO-DEC-0047 item 10; PROCEDURE-MAP Q-L6); tool governance (item 11); the journal-insertion script (Q-L2 signal J); per-client global deny configurations and agy heartbeat hooks (outside the repository, PROTO-DEC-0048 item 8); automatic detection of script candidates beyond counting; linting of prose documents. Reason: none is a blocker of this round, and each needs the kernel design or an owner authorisation.

## Where the round-2 reports conflict

**S-deepseek-16.** 1) `checkStopRule` capability (Copilot vs Gemini): re-read in code this session; the evidence supports Copilot, and S-deepseek-05 adds the missing branch. 2) Clean-tree attainability (Mistral vs my round-2): both understated; the dirty flag counts journals while the digest excludes them, so the label is unattainable in any started session and the digest, not cleanliness, is the binding (S-deepseek-06). 3) Uniform client signals (Qwen vs Copilot, Gemini, my round-2): refuted again by the per-agent row count. 4) Suite 270s vs 315s: the recorded Evidence line at `.ai/worklog/copilot-2db59a7f7eba7f43.md:43` supports 315s; I did not re-run it (S-deepseek-18). 5) Z3-01 certifier-list challenge (Mistral): my round-2 reproduction stands - the challenged item carries no such list, while its candidate-scope content is right. 6) Z2-4 certifier list (Mistral): wrong; PROTO-DEC-0046 item 6 and PROTO-DEC-0047 item 4 exclude DeepSeek there and make Copilot shadow-only. 7) Z4 exit-code 3: rejected, 0/1/2 stays.

## Owner-only questions

**S-deepseek-17.** 1) Confirm the migrated Roles lines and scope ids (S-deepseek-02) and whether codex's dispatch role excludes it from this candidate. 2) Name the second binding certifier for this high-risk round (Codex exhausted; DeepSeek controls, Gemini implements). 3) Are producer and certifier worktrees provisioned this round, and who owns them? 4) PLAN: approve the stale-section trim, or raise the 200-line cap for the budget-exhaustion proposal? 5) K2: approve MIT, withdraw DEC-0014, or grandfather it? 6) Are agy heartbeat hooks and per-client deny configurations sanctioned now or deferred? 7) Authorise the commit(s) needed to freeze the candidate. 8) Confirm the signals-ledger append exemption from the shared-document lock.

## What I did not verify

**S-deepseek-18.** No reproduction script was executed (read-only synthesis); CLI resume combinations come from `--help`, not runs; the watchdog CPU sampling and the quotation matcher are designs, not calibrated code; the validator's installed-role behaviour under the new TASK header was not measured; the 315s figure is a recorded Evidence line I read but did not re-run; and the grammar's effect on installed host projects' `.ai/TASK.md` files is untested.
