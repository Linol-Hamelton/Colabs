# Round 3: independent synthesis (PROTO-DEC-0053 step a)

- Frame `task:vmc-r3-<a|b|c>`, parent-scope `program:validator-migration-council`. Your one role: synthesiser X (owner §24). Read `prompts/COMMON.md`
  first; it binds you.
- The three synthesisers, A, B and C, are of different makers; the owner names them by the README model table. One of them is a fresh
  session of the Anthropic flagship: that synthesiser later writes the draft decision (step b).
  Any synthesiser may be named for the final plan. No session that wrote the proposal or this
  package takes part (COMMON section 6).

## Inputs

1. `round3/CORPUS.txt`. At your start and at your end, check the sha256 of every file it lists;
   a difference is a stop, reported to the coordinator.
2. Every file it lists: all of round 1, the three challenges and the issue matrix.
3. The repository at the baseline, for any FACT you cite.

## Work

Answer the fourteen questions of §24, each with evidence (COMMON section 4):
- where the corpus disagrees, give your resolution and the evidence that decides it;
- a majority is not evidence (owner §27);
- apply the owner-question filter of §29 before any question reaches section 13 of your answer;
- apply the anti-big-bang rule of §31 to your DAG.

## Output

`round3/synthesis-<X>.md` (at most 250 lines).

## Do not

- Open another `round3/synthesis-*` before yours is finished. Record in your journal that you did
  not (PROTO-DEC-0052 item 2). If you did open one, say so: your synthesis is then advisory only
  (the stop condition of S-003).
