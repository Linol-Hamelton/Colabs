# VALIDATOR-CONTRACT-MAP

- Baseline-SHA: a4e6aef86440bc0e8da8f06c8f1d3f65af254367
- Model: deepseek-flash-4.1
- Model-maker: DeepSeek
- Client: Kilo (agent name `deepseek`)
- Effort: unknown (not exposed by this route; owner-assigned slot, tier T8)
- Task-frame / scope-id: task:vmc-r1-a (parent-scope program:validator-migration-council)
- UTC-date: 2026-09-25
- Mode: ADVISORY

Source: full read of `validate-protocol.ps1` at the baseline (1109 lines) plus its consumers in
`VALIDATOR-CALL-GRAPH.md`. `path:line` is the baseline. CLASS is NORMATIVE (required by a binding
source, by a test that encodes the contract, or by a live consumer), ACCIDENTAL (implementation
detail with no authority; parity is desirable but not required), or CURRENT DEFECT (behaviour that
is wrong or inconsistent). "Confound" answers owner §7: would fixing it during the migration
confound the equivalence test?

Fixture note (FACT, measured by this session on a self-contained copy of the baseline tree with a
one-commit git history, 2026-09-25): a positive run printed 148 `[PASS]` lines, `Protocol OK. 0
warning(s).`, exit 0; one injected CRLF into `.ai/TASK.md` produced
`[FAIL] CR/CRLF found; ... .ai/TASK.md`, `[FAIL] .ai/TASK.md: 81 lines exceeds limit of 80`,
`Protocol BROKEN. 2 failure(s), 0 warning(s).`, exit 1. Not recorded in `MEASUREMENTS.md`; a single
run per side, no timing claim.

## Rows

| # | Behaviour | INPUT | STATE / PRECONDITION | EXPECTED CHECK | EXPECTED OUTPUT CLASS | EXPECTED EXIT | SIDE EFFECTS | CALLERS DEPENDING | SOURCE OF AUTHORITY | CLASS | Confound |
|---|---|---|---|---|---|---|---|---|---|---|---|
| CM-01 | Invocation / parameter binding | script path; optional `-Quiet` | PowerShell host, `-File` | `[CmdletBinding()] param([switch]$Quiet)` (`:3-4`); an unknown parameter is a PowerShell binding error outside the check framework | PS error, no `[STATUS]` lines | non-zero (not pinned) | none | CI, tests, docs (all pass only `-Quiet`/nothing; FACT: grep) | code; `AGENTS.md:332` | ACCIDENTAL | no |
| CM-02 | Root resolution | — | script loaded from the protocol root | `$Root = $PSScriptRoot` (`:11`); child processes run with `WorkingDirectory=$Root` (`:40`) | first lines `AI Collaboration Protocol - validation`, `Root: <abs>` | — | none | every output consumer | code; tests pass absolute paths | NORMATIVE | root path must normalise in parity |
| CM-03 | Output encoding | — | any console | force `[Console]::OutputEncoding = UTF8(no BOM)` (`:10`) | UTF-8 text; non-ASCII names readable | — | changes this process's console encoding only | `tests/validator-syntax.test.cjs:38-45` asserts a Cyrillic filename | code comment; AGENTS section 11 | NORMATIVE | strip ANSI / pin no-ANSI |
| CM-04 | Header + final summary | — | — | fixed strings; summary at `:1104-1109` | `Protocol OK. N warning(s).` / `Protocol BROKEN. N failure(s), M warning(s).` | 0 / 1 | none | CI (`protocol.yml:33`), `tests/registry.test.cjs:37`, reviews quoting it, M-01 counting | code; tests; CI; docs | NORMATIVE | exact bytes |
| CM-05 | Status-line protocol | — | — | `Write-Result` (`:19-28`): `[PASS]/[WARN]/[FAIL] <msg>`; FAIL/WARN counters; `-Quiet` suppresses PASS only | one line per event | — | none | CI tokens; all suites' message assertions | code; tests | NORMATIVE | line order |
| CM-06 | Manifest read + required set | `protocol-manifest.json` | leaf file | parse JSON; role default `source`; `$Required = managed + integration + '.ai/'+state (+ source: source + tests + 'templates/ai/'+state)` (`:130-153`) | `FAIL missing file: protocol-manifest.json` / `FAIL protocol-manifest.json is not valid JSON: ...` / per-file PASS/FAIL | 0/1 | none | `record`; installer; fixture seeding | DEC-0012, DEC-0013 | NORMATIVE | no |
| CM-07 | Role semantics | manifest `role` | `source`/`installed` | switch check sets: registry, corpus budget, installer self-check, digest verification, `docs/reviews/` path rule, gate-check skip (`:141,144,211,258,474,700,785,869,954,1078`) | PASS/FAIL/WARN per branch | 0/1 | none | installed hosts and CI install step | DEC-0013; PROTO-DEC-0032 item 7 | NORMATIVE | no |
| CM-08 | Required-file presence | `$Required` | each path resolves to a file | `Test-Path -PathType Leaf` (`:154-159`) | `PASS present: <rel>` / `FAIL missing file: <rel>` | 0/1 | none | every consumer | DEC-0012/0013 | NORMATIVE | no |
| CM-09 | Git availability + root identity | `git` on PATH | — | `rev-parse --is-inside-work-tree` = `true`, `--show-toplevel` resolves equal to `$Root` (`:163-178`); a `.git` directory alone is not accepted (`tests/validator.test.cjs:254-259`) | `FAIL git is unavailable...` / `FAIL not a valid Git working tree...` / `FAIL protocol root is inside another repository` / `PASS valid Git working tree` | 0/1 | spawns git | `record`; every git-dependent check below | tests 254-275 (incl. linked worktree) | NORMATIVE | no |
| CM-10 | Tracked/untracked enumeration | — | valid work tree | `ls-files --cached --others --exclude-standard -z`; worklog directory added separately even when ignored (`:179-195`) | `FAIL cannot enumerate Git files: ...`; paths feed later checks | 0/1 | spawns git | encoding/size/journal/ownership checks | code comment `:191`; test `validator.test.cjs:173-178` | NORMATIVE | enumeration order; ignored-file inclusion is deliberate |
| CM-11 | Git history presence | — | — | `rev-list --all --count`; 0 → WARN; command error → FAIL (`:182-187`) | `WARN no commits yet...` / `FAIL cannot read Git history: ...` / `PASS commits on record: N` | 0/1 | spawns git | reviewers report the count | tests 261-265 | NORMATIVE | no |
| CM-12 | Protocol-owned filter | relative path | — | `.codex/config.toml` never owned; `$Required`; `.github/workflows/protocol.yml`; prefixes `.ai/`, `templates/ai/`, `templates/reviews/`, `.claude/hooks/`; source adds `docs/reviews/`, `docs/decisions/` (`:200-213`) | non-owned files silently skipped | — | none | encoding checks | code comment `:196-199` | NORMATIVE | set membership must match exactly |
| CM-13 | Text-file selection | owned paths | `Test-Path Leaf`, not a reparse point | extension list + special names (`:215-227`) | inspection counted in CM-21 | — | none | encoding checks | code | ACCIDENTAL | a different list changes which files are inspected |
| CM-14 | Symlink / reparse handling | owned text file with ReparsePoint | — | WARN `symlink content not inspected: <rel>` and skip (`:222-225`); corpus walk skips reparse dirs/files (`:277,285`); review paths reject reparse segments (`:107-115`) | WARN / FAIL (path safety) | 0/1 | none | tests 139-171, 483-506, 558-584 | code; tests | NORMATIVE | junction semantics on Windows |
| CM-15 | BOM | file bytes | owned text file | bytes EF BB BF → FAIL `byte order mark present: <rel>` (`:230-232`) | FAIL | 1 | none | AGENTS section 11 | AGENTS section 11 | NORMATIVE | no |
| CM-16 | Strict UTF-8 | file bytes | owned text file | `UTF8Encoding(false,true).GetString` may throw → FAIL `invalid UTF-8: <rel>` (`:233-234`) | FAIL | 1 | none | tests 45-64 | AGENTS section 11 | NORMATIVE | no |
| CM-17 | NUL byte | file bytes | owned text file | any 0x00 → FAIL `NUL byte in text (possibly UTF-16)` (`:235`) | FAIL | 1 | none | tests 51-59 | AGENTS section 11 | NORMATIVE | no |
| CM-18 | CR / CRLF | file bytes | owned text file | any byte 13 → FAIL `CR/CRLF found; use LF line endings: <rel>` (`:236`); probe confirmed | FAIL | 1 | none | tests 173-178; AGENTS section 11 | AGENTS section 11 | NORMATIVE | no |
| CM-19 | PS ASCII-only | file bytes | `.ps1/.psm1/.psd1` owned | byte >127 → FAIL `non-ASCII byte in PowerShell script: <rel>` (`:237-240`) | FAIL | 1 | none | tests 27-36 | DEC-0001 | NORMATIVE | no |
| CM-20 | PS syntax | file path | `.ps1/.psm1/.psd1`; PowerShell available | `[Parser]::ParseFile`; each error → FAIL `PowerShell syntax in <rel>: <msg>` (`:241-245`) | FAIL | 1 | parser in-process | tests 47-64 | §12 capability | NORMATIVE (capability-dependent) | message text is parser-version-specific |
| CM-21 | Inspection summary | — | — | always emitted, even N=0 (`:247`) | `PASS inspected N protocol-owned text files for UTF-8, BOM, LF and PowerShell syntax` | — | none | reviewers quote it; CI ignores | code | ACCIDENTAL wording / NORMATIVE presence | N varies with enumeration |
| CM-22 | Journal count cap | `.ai/worklog/*.md` minus README | — | count >100 → WARN (`:249-254`) | WARN with count and cap 100 | 0 | none | PROTO-DEC-0057 item 5 | PROTO-DEC-0057; PROTO-DEC-0037 WARN-first | NORMATIVE | no |
| CM-23 | Review corpus budget | `docs/reviews/` recursive | role `source` | walk excludes `archive/` and reparse points; >200 files or >2097152 B → WARN (`:256-302`) | WARN `active docs/reviews/ exceeds budget (...)` | 0 | none | tests C40-07 | PROTO-DEC-0057 item 5; AGENTS section 8 | NORMATIVE | byte-threshold unit must match |
| CM-24 | File line limits | text `.ai/TASK.md`, `.ai/PLAN.md`, journals | file exists | `Get-LineCount` = `\n` count +1 when no trailing `\n` (`:66-72`); >80 / >200 / >150 → FAIL `"<rel>: N lines exceeds limit of M"` (`:304-316`) | FAIL; else PASS `<rel>: N/M lines` | 1 | none | every close-out; M-01 Evidence | DEC-0006; AGENTS section 8; archive parity comment | NORMATIVE | counting semantics must be identical |
| CM-25 | CLAUDE.md import | `CLAUDE.md` | file exists | a line exactly `@AGENTS.md` or `@./AGENTS.md` → PASS; else FAIL (`:318-323`) | FAIL `CLAUDE.md must import @AGENTS.md on its own line` | 1 | none | tests 66-81 | DEC-0002 | NORMATIVE | no |
| CM-26 | Claude hook wiring | `.claude/settings.json` | file exists | JSON parse; for SessionStart/Stop exactly one command hook equal to the canonical string (`:325-342`) | FAIL invalid JSON / FAIL `must configure exactly one canonical protocol hook command` / PASS | 1 | none | tests 66-81; T2 rationale | DEC-0003 | NORMATIVE (exact string ACCIDENTAL; installer produces it) | exact string bytes |
| CM-27 | Node presence + hook module syntax | `node` on PATH | — | absent → FAIL; present → PASS with `--version`; `--check` x3 modules (`:344-360`) | FAIL / PASS | 1 | spawns node | tests 79-81; `record` | code; hooks require Node | NORMATIVE presence; missing floor = defect D-2 | version text normalised |
| CM-28 | Codex hook wiring | `.codex/hooks.json` | file exists | exact command per event; matcher `*`/`.*`; no `commandWindows`, no `async`, `timeout=30` (`:362-390`) | FAIL invalid / FAIL exact-one / PASS | 1 | none | tests codex | Codex hook contract | NORMATIVE | exact string bytes |
| CM-29 | Bash presence + wrapper syntax | Git-Bash candidates, PATH | wrappers exist | `--version` success and `GNU bash`; `-n` per wrapper (`:392-418`) | FAIL `working Bash unavailable...` / FAIL `hook shell syntax...` / PASS | 1 | spawns bash | Claude hooks; tests findGitBash | DEC-0003 hook wiring | NORMATIVE | FAIL when no bash: see §12 |
| CM-30 | Decision block structure | `.ai/DECISIONS.md` | file exists | block regex (`:424`); 4-digit id; duplicates; per-block `Status`/`Date`/`Approved by`; Proposed forbidden except DEC-0014; placeholder approvers rejected; `TryParseExact yyyy-MM-dd` (`:425-459`) | FAIL per violation; PASS `inspected N decision blocks...` | 1 | none | every session | AGENTS section 2/6; DEC-0021; PROTO-DEC-0022 | NORMATIVE | regex semantics `.NET`→JS; calendar dates |
| CM-31 | Supersedes references | block bodies | — | every `(PROTO-)?DEC-nnnn` in a `Supersedes:` line must exist and not self-reference (`:434-438,462-469`) | FAIL `X supersedes Y, which does not exist` / `supersedes itself` | 1 | none | governance | code comment; DEC-0013 | NORMATIVE | no |
| CM-32 | Decision registry (WARN-first) | `docs/decisions/REGISTRY.md` | role source | missing → WARN; coverage both ways WARN; committed-row prefix immutability vs `HEAD:` WARN; new block `Reopen-trigger` validity WARN (`:473-563`) | WARN lines; PASS `inspected decision registry with N entries` | 0 | spawns git | tests/registry | PROTO-DEC-0033 items 1-5 | NORMATIVE (level = WARN) | WARN must stay exit 0 |
| CM-33 | Task status | `.ai/TASK.md` | file exists | exactly one `Status:` line; vocabulary (`:565-578`) | FAIL exactly-one / WARN no active task / FAIL invalid / PASS | 0/1 | none | every session; gate | AGENTS sections 3/6; tests 244-252 | NORMATIVE | no |
| CM-34 | Completion gate presence | TASK Status Completed | — | `## Completion gate` section required (`:583-587`) | FAIL | 1 | none | tests 40-43 | AGENTS section 2 | NORMATIVE | no |
| CM-35 | Light-path selection | gate fields `Scope`, `Baseline` | scope docs/config; 40-hex baseline | verify commit + ancestor; raw changed set (exclude only `.ai/worklog/**`, `.ai/runtime/**`, `.ai/TASK.md`); protected check BEFORE review exclusion; review removal; non-empty; scope allowlists (`:597-688`) | selects light or strict; no own output | 1 | spawns git | tests R5, C40-01/02/05 | PROTO-DEC-0038 item 2; PROTOCOL.md light-path contract | NORMATIVE | evaluation order is load-bearing |
| CM-36 | Light-path review checks | gate `Independent review` | light path selected | safe path; source → `docs/reviews/`; exists; non-empty; header region (first `---`, else first `## `); transcribed/ADVISORY/Reviewer/Verdict ∈ {PASS, RECOMMENDATION} (`:690-763`) | FAIL per violation / PASS `independent review certified (light path: ...)` | 1 | none | tests 284-506 | PROTO-DEC-0038 item 2 | NORMATIVE | header-region semantics |
| CM-37 | Strict-path prompt + review checks | gate fields | strict path | both fields, separate artifacts, safe+source path, exists, non-empty; prompt contains "unified ... adversarial ... prompt"; review header: transcribed/ADVISORY/Reviewer/Verdict (`:765-853`); all TASK-cited `docs/reviews/*.md` exist and are non-empty (`:855-868`) | FAIL per violation / PASS lines | 1 | none | tests 40-137 | PROTO-DEC-0038 item 1; PROTO-DEC-0032 | NORMATIVE | phrase regex + citation regex are exact |
| CM-38 | Cited-review scan | whole TASK text | — | regex `(?i)docs/reviews/[a-z0-9._-]+\.md` collects citations (`:855-856`) | missing/empty citation FAIL | 1 | none | tests 89-100 | code | ACCIDENTAL | prose false positives; nested paths not matched |
| CM-39 | gate-check invocation | TASK Completed | role source, node available | `PROTOCOL_SKIP_GATE=1` → PASS skip; else `node .ai/bin/protocol-handoff.cjs gate-check --root <root>`; non-zero → FAIL with the stripped last non-empty line (`:869-889`) | PASS skip / PASS verified / FAIL `gate-check: <msg>` | 1 | spawns node; reads journals | `record` (skip), CI | PROTO-DEC-0032 items 1-8 | NORMATIVE | interface text parsing |
| CM-40 | Hooks globally off | `.claude/settings.json`, `.local.json` | file exists, parses | `disableAllHooks = true` → FAIL (`:893-907`) | FAIL | 1 | none | tests 99-108 | T2 comment; DEC-0003 | NORMATIVE | no |
| CM-41 | Ignore-rule probes | three probe paths | git usable | `check-ignore -q --no-index`; code 0 = ignored; 1 → FAIL; other → WARN (`:909-923`) | FAIL/WARN | 0/1 | spawns git | tests 110-115 | installer ignore contract | NORMATIVE | no |
| CM-42 | Version coherence | manifest, AGENTS.md, installer | manifest has `protocolVersion` | AGENTS heading `## AI Collaboration Protocol v...`, installer `protocol vX.Y.Z`; drift → FAIL (`:925-946`) | FAIL `<file> says X; protocol-manifest.json says Y` / PASS | 1 | none | tests/review-findings 191-200 | DEC-0021 (one version, three files) | NORMATIVE | regexes exact |
| CM-43 | Installed content digests | manifest `contentDigest` | role != source | malformed → FAIL; missing file → skipped; hash equal → continue; doc allowlist → WARN; else FAIL (`:948-986`) | WARN/FAIL; PASS `verified N managed content digest(s)` | 0/1 | file reads | installer/upgrade tests | installer digest contract; `setup-ai-protocol.ps1:250-262` | NORMATIVE; silent skip = defect D-6 | doc allowlist membership |
| CM-44 | Decision immutability vs HEAD | `HEAD:.ai/DECISIONS.md` | git usable, file exists | block bodies normalised (CRLF→LF; trailing `---` and blank lines stripped) compared; changed → FAIL; deleted → FAIL; special case last block; uncommitted file → WARN (`:988-1059`) | FAIL/WARN/PASS `N committed decision blocks are unchanged` | 0/1 | spawns git | tests 622-749; every close-out | DEC-0021; AGENTS section 6 | NORMATIVE | normalisation exact |
| CM-45 | Journal Agent-line | each `.ai/worklog/*.md` | file readable, `Agent:` present | `Agent:` value must contain the filename prefix (`:1061-1075`) | WARN | 0 | none | AGENTS section 5 | AGENTS sections 5/12 | NORMATIVE | no |
| CM-46 | Installer presence + self-check | `setup-ai-protocol.ps1` | — | source: must exist (FAIL); run no-arg self-check through the current shell; exit 0 → PASS; else FAIL with last output line (`:1077-1102`). installed: PASS | FAIL/PASS | 0/1 | spawns PowerShell + git | tests 83-88; CI | DEC-0008; comment `:1086-1089` | NORMATIVE | last-line parsing |
| CM-47 | Exit contract | — | — | 0 iff `Failures==0`, else 1 (`:1104-1109`) | — | 0/1 only | none | all parsers (section 4 of the call graph) | code; tests; CI | NORMATIVE | unhandled exceptions = defect D-1 |
| CM-48 | Side effects | — | — | header states "without changing files"; no repository writes; subprocesses as in call graph; console encoding changed in-process | — | — | process spawns, console state | — | code header `:2` | NORMATIVE | spawning count/perf is zone B |
| CM-49 | Environment dependence | PATH (`git`, `node`, `bash`, bash candidates), `%OS%`, `PROTOCOL_SKIP_GATE`, node version, console/locale | — | `Get-Command` lookups (`:163,344,400`); `$env:OS` for Git-Bash candidates (`:395`); `PROTOCOL_SKIP_GATE` (`:870`); culture-sensitive `Sort-Object -Unique` (`:218,251,305`) | varies | varies | — | §13 matrix | — | H (partly) | determinism of ordering across hosts |

## CURRENT DEFECTS

| # | Defect | Evidence (baseline) | Impact | Confound if fixed during migration |
|---|---|---|---|---|
| D-1 | Terminating exceptions outside the check framework abort the process before any `[FAIL]` line or the summary: `Get-Sha256Hex` on an unreadable file (`:74-86` called at `:977`), `Get-Item` on an access-denied path (`:221`), `(Get-Process -Id $PID).Path` (`:1090`, null handled, throw not) | code; `$ErrorActionPreference = "Stop"` (`:6`); no `try/catch` at these sites | a gate parser that greps `[FAIL]` sees a broken run as "no failures"; exit code is still 1 (INFERENCE from `-File` semantics) | Only if a fixture exercises unreadable/ACL paths. Keep such fixtures out of the parity corpus, or pin the crash as expected output for both engines. |
| D-2 | Three different Node floors: validator accepts any `node --version` (`:347-350`); `test-protocol.ps1:13` requires >= 22; `.ai/bin/protocol.cjs:43` says ">= 18.0.0 required" | code | policy drift; a host with Node 18-21 passes validation but cannot run the suite | Yes: changing the validator floor changes verdicts on old-Node hosts. Decide the floor once (owner §29: deterministic derivation from code is possible only after the drift is adjudicated) and pin it in fixtures. |
| D-3 | `Test-ProtocolSafePath` rejects any path containing `..` as a substring, including benign names such as `notes..md` (`:92`); `isSafeInRoot` mirrors it (`protocol-handoff.cjs:893`) | code; tests only cover traversal forms | over-blocking of a legal review path; no false pass | Yes, for fixtures that use such names. Fix both engines together and pin the new verdict. |
| D-4 | `$textExtensions` uses only the final extension (`:227`): `x.ps1.txt` is not PS-checked; a text file with an unlisted extension is skipped | code | a renamed protocol script can escape DEC-0001 | Yes: it changes which files are inspected. Record as an explicit non-goal or fix in both engines with fixtures. |
| D-5 | Block detection and immutability parsing terminate only at `#{1,3}` headings (`:424`, `:996`); a `####` heading inside a block does not terminate it, and a body that quotes `Status:`/`Approved by:` lines can trigger duplicate-field FAILs | code; tests only exercise `###`/`##` | parser false positives on unusual bodies; corpus currently passes (0 warnings at baseline) | Yes: parser changes move both the structure check and the immutability check. Keep frozen for phase 1, or pin exact outputs. |
| D-6 | Installed digest loop skips a missing scheduled file silently (`:975`) and does not count it; only the required-file check catches absence | code | the `verified N` figure overstates coverage; no false green for missing files overall | No for normal fixtures (required-file check dominates); a parity fixture must still assert the `N` count. |

## Behaviour families explicitly requested by owner §7 and where they live

manifest validation CM-06/CM-07; required-file validation CM-08; source vs installed role CM-07;
Git worktree validation CM-09/CM-10/CM-11; tracked/untracked enumeration CM-10; protocol-owned
files CM-12/CM-13; encoding CM-03/CM-15..CM-19; BOM CM-15; UTF-8 CM-16; CR/LF CM-18; PowerShell
syntax CM-19/CM-20; safe paths CM-14/CM-35/CM-36/CM-37; symlink/reparse CM-14; journal limits
CM-22/CM-24/CM-45; review corpus limits CM-23; decisions CM-30/CM-31/CM-44; supersedes CM-31;
gates CM-34..CM-39; evidence CM-21/CM-32/CM-47; review/certification semantics CM-36/CM-37/CM-39;
protected paths CM-35; environment-dependent behaviour CM-49.
