# Worklog: deepseek-55173c7a252994d3

Session journal. Owned by this session. No other session writes here.

Newest entry first. Limit 150 lines.

---

## 2026-09-25 - Package L third pass: FAIL, the push block falls to a competing `insteadOf`

Agent: deepseek-55173c7a252994d3 (DeepSeek `deepseek/deepseek-flash`, effort unknown, Kilo mode Code, local
Windows). CORE-ARCH reviewer; certifies none of the stages; this report answers the launch line of
`docs/reviews/2026-09-25-claude-core-arch-stage2-fix-response-addendum-2.md`.

Action: Independent third-pass review of package L at candidate `9a936dd` (L paths byte-identical to HEAD and the
working tree; `git diff --quiet 9a936dd HEAD -- <L paths>` exit 0). Full report:
`docs/reviews/2026-09-25-deepseek-core-arch-stage2-review-packageL-third-pass.md` (215 lines, per
`templates/reviews/REVIEW.md`). Ran `node --check` (3 files, 0), `launch-test.cjs --pure` (0, 55/55),
`launch-test.cjs` full (0, 80/80), `launch.cjs --check researchers` (0, 18/18), `--check` all (0, 24/24),
`--preflight` (0, 9/9, nothing written), `--status` (0), `--dry researchers` (0); no model called. Adversarial
probes in `%TEMP%\kilo\l3-probe.cjs` and `l3-probe2.cjs` (scratch repositories only). Verified F-L2 root cause
closed by the private clone (self-test `zz-t16`, checkout config/refs untouched), F-L3/F-L4/F-L5 recorded in
`P-L3-004:96-100` and `K-launch.md:88-92`, and IF-1 fixed in step 1 of all four prompts. Recorded, and did not
answer, the open points.

Result: **FAIL**. F-L1 is not closed. The empty-prefix `url.no-push://blocked.insteadOf` rule is always the
shortest match, so a longer `insteadOf` supplied by `-c` or by the ambient system/global config wins: reproduced
`git -c url.<file-url>.insteadOf=no-push://blocked push no-push://blocked HEAD:...` -> exit 0, the remote gains
the ref, and the clone's config, refs, HEAD, files and hooks are all unchanged, so the scope check and the owner's
`git status`/`git remote -v` cannot see it. This contradicts R-L3-004.9 ("rewrites every URL git resolves") and the
risk row's "the scope stop comes after the fact". Other findings: F-3P-2 the shared writable
`%TEMP%\colabs-research\.no-hooks` is outside every scope check and a planted `post-checkout` hook runs in the
launcher's own `gitIn` call (reproduced); F-3P-3 the scope check misses index concealment
(`assume-unchanged`/`skip-worktree`); F-3P-4 K-launch step 0 and README status still gate on the failed second
pass; F-3P-5 a transient index lock can produce a spurious `SCOPE_STOP`. Each finding carries a reproduction in the
report. No L path was edited by this session.

Next step: The owner or the implementer applies one of the two F-3P-1 remedies (narrow the R-L3-004.9 claim and
record the `-c`/ambient residual, plus two push-block checks; or a config-independent control) and fixes the low
points; then a re-reviewed commit can satisfy the K-launch gate. Open points are in the report and must not be
silently dropped.

Open:
- F-3P-1 is the blocking item; the second pass's HIGH F-L1 is effectively reopened at its remedy, not at a new root
  cause.
- F-3P-2 (shared `.no-hooks`), F-3P-3 (index), F-3P-4 (pass wording), F-3P-5 (`GIT_OPTIONAL_LOCKS=0`) are recorded
  and unaddressed at this candidate.
- Not verified here: the real clients in a clone (`--smoke` runs in a plain temp dir), the effect of `insteadOf` on
  a client's own network use of git, and the copilot deny-tool effect.

Evidence:
- anchor: 5ace76c6ed76500740f22a7649c1cf9e535541b3, uncommitted changes present
- digest: sha256:77579b702f9b1f684f5466834287f9be74c9bffc4ea95e3c485f3b38de554ae8 over 460 tracked and untracked files
- digest format: 4
- recorded: 2026-09-25T09:51:24.965Z by deepseek-55173c7a252994d3
- entry hash format: 2
- entry: sha256:bfd328ffc5d0179d25310d3223bf4bb1091032bd1301ef6dfd60b688fc5ff6d2 of this entry without this block
- parent-entry: root
- scope: protocol checks only; host-project tests run separately
- validate-protocol.ps1: exit 0 in 3s
- test-protocol.ps1: exit 0 in 294s
- reproduce: node .ai/bin/protocol-handoff.cjs verify
